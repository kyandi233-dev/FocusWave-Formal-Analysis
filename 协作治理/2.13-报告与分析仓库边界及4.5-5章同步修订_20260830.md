# 报告与分析仓库边界及 4.5—5 章同步修订（2026-08-30）

## 1. 写入边界

本项目后续只向两个自有仓库写入报告与分析说明：

- `kyandi233-dev/Attention-Analysis@codex/formal-analysis-v2-portable`：Behavior、NIR、RGB 正式分析代码与分析说明；
- `kyandi233-dev/FocusWave-Formal-Analysis@codex/code-fix-ledger`：正式报告正文、分析设计、证据映射与治理记录。

`greenboo26/focuswave-multimodal-attention-analysis@main` 仅作为毫米波和多模态方法的只读权威来源。不得向该仓库写入本项目正式报告、写作要求、分析设计或治理文件。

## 2. Attention-Analysis 同步内容

本轮重新核验远端后，继续在既有 `codex/formal-analysis-v2-portable` 分支修改，没有新建分支。已完成：

- 更新 `docs/060-formal-analysis/001-正式多模态V2路径与分析契约.md`，明确当前 Behavior/NIR/RGB 范围、RGB 轻量路线、毫米波外部只读来源和多模态结果边界；
- 新增 `docs/060-formal-analysis/README.md`，建立 001—006 的正式分析文档索引；
- 保留 `006-正式报告方法与结果权威来源_20260830.md` 作为报告写作时的最新来源说明。

RGB 当前正式范围为 Motion Energy 与曝光变化分轨、必要的 Pose 运动确认/方向候选、独立 Blink 候选；AU、情绪、rPPG、PERCLOS、复杂 RGB 预测和正式多模态融合不属于当前 RGB 主线。

## 3. FocusWave-Formal-Analysis 同步内容

正式报告第 4.5 节已依据最新分析来源再次修订：

- Behavior 使用简洁的 Go 遗漏率、真遗漏率、预判遗漏率；
- NIR 继续采用 pupil-only 直接几何，并把实验过程中的典型水平、事件前短时水平和条件式静息参考分开表述；
- RGB 删除历史大而全范围，只保留整体运动/曝光、姿态方向候选和眨眼候选事件；
- 毫米波从已经完成距离向处理的 8 通道×256 距离单元复数距离域信号开始描述，不再写下游再次执行距离 FFT；
- 跨模态使用共同探针窗口和留一参与者交叉验证，在相同测试参与者上比较八种模态组合；复杂 AI 融合仅作为次级分析。

同时更新 `分析设计/1.7` 和 `1.8`、`分析设计/README.md` 以及 `正式报告/章节草稿/README.md`，使结果结构、分析说明和正式正文使用同一套方法口径。

## 4. 外部证据读取范围

本轮只读核验的外部方法证据包括：毫米波正式管线逐段科学审计、毫米波逐帧时间语义、多模态融合文献比较、多模态增量/互补分析设计、多模态 AI 融合设计，以及当前 Behavior+NIR+RGB 共同样本的建模准备证据。

其中阶段性共同样本和“具备建模条件”只用于说明当前分析进度，不作为正式科学结果；毫米波未进入当前 Behavior+NIR+RGB 的共同特征集合，正式多模态结果仍需后续真实模型运行。

## 5. 第二遍复核重点

本轮报告复核重点不是工程状态，而是正文是否清楚回答“测量什么、如何形成变量、如何控制主要混杂、如何处理重复观测、怎样比较模态增量”。第二遍具体删除或改写了：旧 RGB 大范围指标、旧毫米波下游距离 FFT 表述、过度工程化的 NIR 基线术语，以及把阶段性准备状态误写成正式结果的风险表述。
