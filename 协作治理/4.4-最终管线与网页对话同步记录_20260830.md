# 最终管线与网页对话同步记录（2026-08-30）

## 一、目的与证据范围

本记录把两条 GitHub 分支的实际提交、两段网页对话中的最新用户决定和本地资产状态对齐，作为后续代码修复、真实数据运行和方法章撰写的共同入口。网页对话内容只作为用户决定和任务状态来源；代码是否实现以 Git 提交和当前文件为准，科学结果是否成立仍以本地真实数据运行与验收为准。

本次核验来源：

- `kyandi233-dev/Attention-Analysis@codex/formal-analysis-v2-portable`；
- `kyandi233-dev/FocusWave-Formal-Analysis@codex/code-fix-ledger`；
- 网页对话“修改分析管线”（会话 `6a92f402-fd8c-83e8-bc7d-a3ec1386c4ad`）；
- 网页对话“融合引言与报告内容”（会话 `6a92a67b-0ec8-83e8-b4cc-42e7eb35e128`）；
- 本机 `D:\_AttentionData\Beijing-NIR\amd-directml` 和项目早期设备/实验说明。

## 二、分支与提交冻结

| 角色 | 分支 | 本次核验完整提交 | 状态 |
|---|---|---|---|
| 正式下游代码 | `Attention-Analysis@codex/formal-analysis-v2-portable` | `7b473782ffd8faf3b137a152117227341181d8d8` | 唯一正式下游默认基线；已建立本地分离提交工作树审阅 |
| 计划、修复记录与报告草稿 | `FocusWave-Formal-Analysis@codex/code-fix-ledger` | `1e6568dc76fed905f25ace94bccd8cbdacf971d5`（本轮编辑前） | 已从 `21b97dd` 安全快进 66 个提交；本轮继续在同一分支补计划和同步记录 |

代码主线相对 `analysis/multimodal-integration` 的共同祖先为 `b2a96c0df6e6db88c599332815fb24cc93c07fe0`。后续提交已经依次完成便携路径注册、44/38/6样本契约、严格合并键、行为科学分析第三版、NIR `fullclass-final` 仅瞳孔适配、分阶段表与验证、候选指标层、事件响应、视觉暴露与调整审计、模型失败门和参与者互斥预测契约。因此 `analysis/multimodal-integration` 仍是历史来源与分支祖先，不再是最终正式分析入口。

## 三、当前代码已实现与仍需修复

| 模块 | 已进入当前代码提交 | 仍需闭环 |
|---|---|---|
| 身份与样本 | `session_id` 与 `repeat_participant_id` 分离；44场、38组、6个双场契约；参与者互斥折与合并键 | 接入最终 `participant_key`、真实 `visit_order`、问卷左连接和问卷缺失状态；与当前44场清单做重叠和分组一致性审计 |
| Behavior | 试次/探针/周期/区块/场次/参与者组多尺度；RT水平、离散、变异、斜率；Go遗漏与No-Go误按分母分开；d′、c、β；Q1名义、Q2有序；B1/B2、时间过程、候选结局、失败表和参与者互斥预测 | 正式主表仍以程序原始遗漏为主；需按有效反应、真正无反应、刺激前提前反应、超窗反应、无法分类建立互斥错误类型，同时保留原始记分，禁止双重计数 |
| NIR | 上游 `fullclass-final` 仅瞳孔适配；唯一来源表；阶段 `10→11→formal→12`；候选几何、动态特征、严格探针前窗、视觉暴露、事件响应、组内/组间分解、调整审计和失败门 | 44场当前生产资产须由同一提交重新物化和验收；场次×眼中心化参考值、事件前局部基线和静息基线需在字段/报告中明确区分；候选端点仍需真实数据冻结 |
| 图表 | Behavior中文图包、NIR Figure01–10及图清单接口 | 正式图片仍存在内嵌标题；需题注外置，并建立“指标×尺度×科学问题×描述/QC/推断图”的机器可读覆盖矩阵 |
| 毫米波 | 正式下游配置只规定外部生产器接入、39场可加载/33组和5场结构性缺失 | 原始生产与正式分析在另一台电脑完成；本机不迁移毫米波，等待标准结果包、清单、字段结构和运行证据 |
| 推断与预测 | 组内/组间分解、参与者聚类和参与者互斥预测契约已经存在 | 正式结果必须分别报告解释性推断和预测泛化；预处理、选择和调参在训练折内完成，并由真实折审计验证 |

## 四、网页对话新增的硬要求

“修改分析管线”对话最新一轮已确认：所有补充继续在现有两条分支完成；修改前和修改后各进行一次完整审查；错误遗漏需要进一步拆型；正式图内不得绘制标题，必须建立图表覆盖审计；瞳孔参考值要明确区分场次×眼中心化、事件前局部参考和静息基线；解释性推断、预测泛化、以参与者为重抽样单位的不确定性、调整前后模型、失败表和分析溯源要用中文清楚说明。网页对话当前停在用户“进行下一轮”，这些新增要求尚不能视为已全部写入代码。

“融合引言与报告内容”对话已经完成引言、文献综述2.1–2.5和第3章研究问题的融合及第二遍语言/证据审查，下一步进入第4章方法。用户最新补充的方法章事实为：

- 最终样本和重复身份从即将冻结的问卷与重复参与资料读取，不能沿用旧31场注册表作为硬门；
- 当前没有伦理审批信息，报告不得补造机构或批准编号；
- FocusWave 内部口径为3.1.4，但竞赛报告正文没有必要写内部版本号。当前分析配置仍记录3.1.3，因此修改配置前必须核对正式程序源码、实际行为文件和变更记录，不能仅凭文字静默改版本；
- 实验环境、照明、亮度、位置和距离均按受控条件固定，具体数值从实际设备/采集说明核验；
- 近红外和毫米波型号优先读取项目早期 `020-Experiment/Intergration/设备参数.md` 与 `数据采集规范.md`；
- 正式探针原文和练习试次以正式实验程序及历史分支实际源码为准：预实验阶段一核对 `stablemsmf` 的 ABCCBA 最终提交，阶段二核对 BBB 最终提交，正式实验核对 `formaltest` 的最终3.1.3/3.1.4实现。

## 五、下一轮执行顺序

1. 完成修改前完整性矩阵：分析要求、当前实现、风险、修改位置、真实数据验证门。
2. 在 `codex/formal-analysis-v2-portable` 同一分支补身份/问卷适配、遗漏互斥分类、正式无标题图表和覆盖矩阵，并补合成回归测试。
3. 完成修改后完整性矩阵，重新检查未考虑逻辑，不以“测试文件存在”替代测试通过。
4. 在干净工作树记录完整提交、解释器和配置，先运行测试，再用1–2个真实场次做 Behavior/NIR完整试跑；逐表、逐图和失败表验收通过后，才扩展到44场。
5. 另一台电脑独立完成毫米波生产并返回标准包；本机收到后只做契约审计与融合接入。
6. 方法章先完成章级结构和事实表，再逐节写作；伦理缺失、内部版本冲突和样本身份未冻结处保留明确占位，不编造。

## 六、状态边界

当前结论是“最终代码主线已确定并完成大部分科学分析接口，但尚未达到真实44场正式运行放行”。代码已提交、合成测试代码存在、本地测试通过、真实小样例通过、44场全量完成、报告可用和科学结论成立是六个不同状态，后续台账必须分别记录。
