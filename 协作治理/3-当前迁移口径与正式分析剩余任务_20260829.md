# 当前迁移口径与正式分析剩余任务（2026-08-29）

> 本文件冻结 2026-08-29 当前对话中已经反复核对、纠正和确认的迁移口径与后续正式分析任务。目的不是增加新的分析路线，而是防止后续网页任务再次把历史分支、被判退结果、当前底层提取结果和正式下游分析主线混在一起。
>
> **本文件优先记录本轮已经纠正过的口径。若早期台账、旧对话或历史分支说明与本文件及 `Attention-Analysis@codex/formal-analysis-v2-portable` 的当前治理文件冲突，必须先重新核验远端 HEAD 与现行治理文件，不得直接沿用旧说法。**

## 1. 唯一正式下游分析主线：必须统一口径

### 1.1 当前正式分析代码仓库

仓库：

`https://github.com/kyandi233-dev/Attention-Analysis`

当前唯一正式下游分析主线：

`codex/formal-analysis-v2-portable`

2026-08-29 本轮再次核验的远端 HEAD：

`e1b79c05029f70a73230d57d8265952fe728a71b`

其父提交 `449c03855999792e933775be0fbe8464abcf2e3f` 已包含 PR #29 的正式收口；`e1b79c...` 继续补充了分支治理说明。

正式治理文件：

- `docs/060-formal-analysis/004-code-fix-ledger放行矩阵_20260829.md`
- `docs/060-formal-analysis/005-当前分支角色与资产保全说明_20260829.md`

其中已经明确：`codex/formal-analysis-v2-portable` 是 Behavior + NIR + RGB + mmWave 外部接入/融合的**唯一正式下游分析默认基线**。

### 1.2 本轮必须纠正的分支混淆

以下分支**不能**被说成当前完整正式分析主线：

- `codex/nir-formal-v2-rejected-rework`：已被正式路线否定的历史 NIR rework；不能用于当前重分析。
- `codex/behavior-formal-v3-rejected-baseline-fix`：属于另一仓库/行为模块的修复支线，不能替代 `Attention-Analysis@codex/formal-analysis-v2-portable` 作为整套正式分析入口。
- `analysis/multimodal-integration`：是较早期跨模态/NIR-Behavior/视觉属性的历史上下文和来源基线；formal v2 已取代它作为未来正式分析默认基线。不能再把它描述为当前唯一正式下游入口。

必须区分：

- **生产提取分支/仓库**可以继续独立存在（例如 AMD/NVIDIA NIR 提取、RGB raw-first、毫米波 producer）；
- **正式下游分析与跨模态合并**统一以 `codex/formal-analysis-v2-portable` 为默认基线。

### 1.3 `协作治理/1-代码修复任务台账.md` 是否已经被读取和落实

已经读取。其主要阻断项通过 Attention-Analysis PR #28、PR #29 进入 `codex/formal-analysis-v2-portable`：

- PR #28：NIR runtime provenance、join-key dtype/time contract；
- PR #29：Behavior science-v3、pupil-only staged NIR、Figure01–10、失败门、报告/预测准入、融合单位、ledger release gates。

因此后续任务不得重新开一条“总分析分支”来重复实现这些要求。应在 `codex/formal-analysis-v2-portable` 上继续收口/修复剩余入口与真实运行问题。

## 2. 当前迁移总目标

迁移盘：

`E:\_Analysis-needed`

原始正式实验数据仍在同一块 E 盘：

`E:\正式实验`

迁移目标是：

1. 不重复复制已经在 E 盘的原始数据；
2. 只迁移 D 盘上不可轻易重建或计算成本高的生产/派生资产；
3. 被判退的旧统计结果不冒充当前正式结果；
4. 保留 provenance、manifest、schema、QC、失败表和校验值；
5. 目标机加入约 75 个新增 session 后，使用**同一版本代码**统一生成约 119 场的分析表，并重新建立全量匿名参与者映射。

### 2.1 所有迁移命令的安全边界

本轮给出的 `Copy-Item` 与 `robocopy /E` 均是**复制**，不是剪切/移动。

禁止在迁移任务中使用：

- `robocopy /MOVE`
- `robocopy /MOV`
- `robocopy /MIR`

源文件必须继续留在 D 盘原位置，除非用户另行明确授权删除或移动。

## 3. Behavior：当前冻结口径

### 3.1 原始 Behavior 不需要重复迁移

正式 Behavior 原始数据已经在：

`E:\正式实验\sub-XXX_\beh\`

因此**不要**再复制一份到 `E:\_Analysis-needed\01_BEHAVIOR\01_raw`。之前提出把所有 `beh` 复制进 `_Analysis-needed` 的方案已被用户纠正，现已废止。

目标机携带同一块 E 盘时，原始 Behavior 直接从 `E:\正式实验` 读取。

### 3.2 旧 `050-sart-formal` 的身份

路径：

`D:\_AttentionData\output-v2\050-sart-formal`

它不是预实验，而是 **2026-08-16 的旧正式 SART 行为分析**：

- 旧报告主样本约 19 名有效参与者；
- 属于旧正式分析版本；
- 不符合当前 44 session / 38 匿名组 / 6 双场重复组口径；
- 不能进入当前 44 场或未来约 119 场正式合并。

若保留，只能放入历史区并明确标记，例如：

`E:\_Analysis-needed\01_BEHAVIOR\03_history\formal-20260816-19sub`

不能放入 `current-derived` 或当前正式结果目录。

### 3.3 PR22 / behavior science v2 的 44-session 派生包

路径：

`D:\_AttentionData\output-v2\050-sart-formal-pr22-final-isolated`

它确实已经生成 44-session 派生表，例如：

- `trial_metrics.csv`：trial 级；
- `window_metrics.csv`：probe 前窗口/固定窗口；
- `phase_cycle_metrics.csv`：phase/cycle；
- `block_metrics.csv`：block；
- `session_metrics.csv`：session；
- `error_trajectory_metrics.csv`：错误事件轨迹。

但该包 manifest 明确属于旧 v2/PR22，并非当前最终 v3 冻结结果；旧模型、图和报告不能直接作为正式结论。

当前冻结处理：

- 原始行为 CSV、索引、哈希可以继续作为复核依据；
- trial 级派生表可作为审计/回归输入，但不能未经检查直接当未来 119 人正式合并表；
- window/block/session/cycle/error trajectory 等派生表应由修复后的统一代码重新生成，或逐字段正式验收；
- PR22 混合模型、候选指标冻结、图表和报告不能直接进入正式报告；
- participant-disjoint 预测需要按当前/未来全量匿名参与者组重新折分并重跑。

最稳妥方案：未来把当前 44 场与新增约 75 场使用同一冻结版本重新生成 Behavior 标准化结果，避免两批数据使用不同定义、窗口和异常值规则。

### 3.4 当前 44 场唯一正式重复被试匿名映射

正式分析当前直接使用：

`D:\_AttentionData\anonymous_participant_mapping_phase0_1_20260829\anonymous_session_participant_mapping.csv`

当前已核验口径：

- 44 行 / 44 session；
- 38 个 `anonymous_participant_group_id`；
- 6 个匿名组各有 2 个 session；
- 不包含姓名、手机号等明文身份字段。

当前冻结 SHA-256：

`A5579351194C741536A3F90041A5FF63B5C8F14C30A90ABEA5D8A4FB4BBCF47B`

相关 manifest 当前冻结 SHA-256：

`DB7BA0EB4CAE968FF60055C8648A3DAA4451CD0A51307DEA1104BD2A1F4A103C`

当前已明确使用的核心字段至少包括：

- `session_id`
- `anonymous_participant_group_id`
- `identity_status`
- `evidence_source_code`
- `confidence`
- `conflict_status`
- `source_verification_value`

**重要：本轮对话中关于 `session_order` 出现过表述冲突。早先现场核验的列清单没有 `session_order`，后续迁移说明里一度把 `session_order` 作为“大致字段”提及。因此后续代码任务必须先读取实际 CSV schema，禁止凭聊天记忆假定它存在。**

如果实际 CSV 没有可验证的 visit/session 顺序：

- B1–B2 属于 session 内配对，不应因为缺少 visit order 而被阻断；
- 只有“第一次参加 vs 第二次参加”这种纵向分析才需要明确 visit order；
- 不得从 `session_id`、时间戳、registry token 顺序自行推断 visit order；缺少真实顺序时该纵向分析必须 fail closed。

### 3.5 当前 44 场映射不能扩展为未来永久身份表

这张映射只用于当前 44 场审计/重复测量分组。

未来新增约 75 场后：

- 不能把新 session 简单追加到当前 CSV；
- 必须把新旧全部约 119 场一起重新做身份核验；
- 重新生成全量匿名 participant mapping；
- 重新生成 participant-disjoint folds；
- 当前 38 组/6 重复组不能被硬编码为未来最终人数。

### 3.6 当前 Behavior 剩余代码任务

在 `Attention-Analysis@codex/formal-analysis-v2-portable` 上继续完成/核验：

1. 正式入口直接兼容上述 `anonymous_session_participant_mapping.csv`，并将 `anonymous_participant_group_id` 规范映射为统计层使用的 participant cluster/repeat participant key；不得引入第二套身份映射。
2. 核验 raw Behavior → trial/probe/cycle/block/session/participant-group 标准化结果是否已经能从一个统一 runner 完整重建；若仍需手工串多个旧脚本，则收口为明确的一键/少命令入口。
3. 输出统一 manifest、schema、QC、failure table、SHA-256 和中文指标说明。
4. 必须保持 Go omission 与 No-Go commission 分开；保留各自机会数/分母；输出 RT mean/median/SD/MAD/IQR/CV/Theil–Sen slope、d′/c/β。
5. probe 主窗口一事件一主行；10/20/30 秒只能作为分离敏感性，不能扩大独立 probe N。
6. cycle 嵌套 block/session/participant；B1–B2 先 session 内配对，再按 participant cluster 做不确定性；错误轨迹 omission/commission 分开并做被试内中心化/基线变化。
7. Q1 四类名义、Q2 四级有序重复测量；模型失败写结构化失败表，不得用空成功结果代替。
8. participant-disjoint 预测按匿名 participant group 分组，禁止同一人两场分别进入 train/test。
9. 真实数据运行顺序：小样本 smoke → 验收 → 当前 44 场正式重跑；未来扩展后再统一约 119 场重跑。

## 4. NIR：当前冻结口径

### 4.1 旧 NIR 结果哪些不能再用

不能作为当前正式结果：

- 旧 PIR；
- 旧虹膜几何；
- 旧 `320×160` 路线的已知无效/验证性结果；
- 旧 `nir-behavior-v2` 验证表和旧 PIR 图/统计；
- 本地临时 `run_nir_formal_analysis.py` 的临时四图/临时统计；
- 未走完整 staged formal pipeline 的临时下游表；
- 旧 `nir-rgb-fusion-validation-v2` 不能直接当未来 119 人最终合并表。

这些资产可用于回归、字段参考、时间匹配检查和 provenance，但不得直接进入当前主结果。

### 4.2 当前可以保留并继续使用的底层 NIR

迁移目标：

`E:\_Analysis-needed\02_NIR\02_RITnet_latest`

它保存的是当前 44 场 topology/当前冻结 pupil geometry 的逐帧 RITnet fullclass-final 输出，可作为重新运行正式 NIR 下游分析的底层输入。

每场应完整保留对应目录，而不是只挑某一列。典型资产包括：

- `eye_metrics.csv`
- `frame_coverage.csv`
- `qc_index.csv`
- `manifest.json` / completion/provenance 资产
- topology/来源/版本信息

当前科学边界：

- 正式 NIR 主线为 pupil-only；
- PIR/OAR 不得因为存在 iris fraction 字段而恢复为正式虹膜几何指标；
- `hard_iris_fraction` / `soft_iris_fraction` 是分割/QC 类字段，不是虹膜直径。

### 4.3 已迁的 NIR 两个层级

`E:\_Analysis-needed\02_NIR\01_YOLO_v1`

- 第一版正式 YOLO-era NIR 输出；
- 保留作历史/对照输入，不作为当前 pupil-only 主结果。

`E:\_Analysis-needed\02_NIR\02_RITnet_latest`

- 当前最新 authoritative topology pupil geometry / RITnet fullclass-final 底层结果；
- 后续正式 NIR 下游分析从这一层重建。

当前已知来源拼接逻辑（迁移时已按 current authoritative source 选择）：

- `sub-031`：`D:\_AttentionData\Beijing-NIR\amd-directml\validation\pupil-geometry\sub-031`
- `sub-032` / `sub-033`：`D:\_AttentionData\Beijing-NIR\amd-directml\ritnet-fullclass-final\sub-032|033`
- 后续批次：`D:\_AttentionData\Beijing-NIR\amd-directml\historical-yolo\ritnet-fullclass-final\sub-XXX`

不得把 `_archive` 当当前结果来源。

### 4.4 当前 NIR 剩余任务

使用 `Attention-Analysis@codex/formal-analysis-v2-portable` 已收口的正式链：

`fullclass-final → pupil-only adapter → 10_analysis_ready → 11_analysis_tables → formal pipeline → validation / Figure01–10`

需要在真实数据上重新生成并验收：

- trial 级 NIR 表；
- probe 前主窗口与敏感性窗口；
- cycle/block/session/participant-group 层；
- session-eye baseline pupil；
- 相对基线变化；
- pupil 变化斜率/波动性；
- 有效覆盖率与 QC；
- current/previous stimulus visual covariates 的严格时间方向；
- 与 Behavior 一致的主键和窗口；
- 按匿名 participant group 的重复测量与 participant-disjoint 分割；
- Figure01–10 数据、失败表、provenance 和 validation manifest。

`nir-rgb-fusion-validation-v2` 只保留为回归/中间资产，不是最终正式合并表。

## 5. RGB：当前冻结口径

### 5.1 当前正式 raw-first 生产结果

源根：

`D:\_AttentionData\Beijing-RGB`

迁移到：

`E:\_Analysis-needed\03_RGB`

当前迁移只保留正式 raw-first subject 输出和根目录 metadata；不要复制 `_test`、benchmark、旧诊断或 `_archive` 作为正式输入。

原始 RGB AVI 已在 `E:\正式实验`，不需要重复迁移。

每个 `sub-XXX` 的 raw-first 目录应整体保留，典型文件：

- `sub-XXX_face_frames.csv`
- `sub-XXX_face_prepare_manifest.json`
- `sub-XXX_face_raw.parquet`
- `sub-XXX_face_raw_manifest.json`
- `sub-XXX_motion_raw.parquet`
- `sub-XXX_motion_manifest.json`
- `sub-XXX_pose_landmarks.parquet`
- `sub-XXX_pose_manifest.json`
- `sub-XXX_manifest.json`

根目录 metadata 包括 `rgb_inventory.csv`、`rgb_duplicate_subjects.csv` 等。

`rgb_duplicate_subjects.csv` 是 RGB 文件来源/重复候选审计，**不是正式重复参与者身份映射**，不得替代 `anonymous_session_participant_mapping.csv`。

### 5.2 RGB 为什么要先在本机完成标准化分析再迁移

当前迁移的是 raw-first 模型输出，不是最终便于 Behavior/NIR 合并的标准化 RGB 表。

本轮记录中，统一的高层 pose derived 产物还不完整；例如只有少数场次出现现成 `pose_features.parquet`，其余主要保留 raw pose landmarks。因此不应直接把各场次现状混合成正式多模态表。

应在当前已配置好的 RGB 环境先对 44 场统一派生并审计，再迁移标准化结果包。

### 5.3 RGB 第一阶段建议统一生成的指标

至少包括：

- 有效人脸覆盖率；
- 面部检测缺失率；
- 眨眼候选率及其质量；
- 头部位移和转动幅度；
- 大幅异常体动比例；
- 姿态地标覆盖率；
- 姿态变化幅度；
- AU 字段覆盖与可信度审计；
- 每个共同时间窗的均值、变异性、趋势和事件率。

当前建议：

- blink/eyelid 类指标可作为较优先候选；
- 头动、姿态、AU 初期先作为 QC/协变量/候选，不要未经验证就全部塞进正式注意力模型。

### 5.4 RGB 标准化输出要求

在本机统一运行 44 场后，至少迁移：

- trial/probe/cycle/block/session/participant-group 级标准化表（能合理定义的层级）；
- 44 场 coverage 表；
- failure table；
- schema；
- 代码 commit；
- 配置/环境记录；
- output manifest；
- SHA-256；
- 中文指标说明。

目标机应能直接使用这些标准化结果与新增约 75 场同结构结果合并，而无需重新处理当前约 16 GB 的逐帧 RGB raw-first，除非算法定义再次改变。

## 6. mmWave：本轮迁移明确排除

毫米波正式数据和环境在目标电脑，本次 `E:\_Analysis-needed` 迁移包不要求携带毫米波生产结果。

仍需保留的代码/治理边界：

- 毫米波 producer 权威仓库：`https://github.com/greenboo26/focuswave-multimodal-attention-analysis`
- `Attention-Analysis@codex/formal-analysis-v2-portable` 只负责毫米波标准结果的治理接入与跨模态融合合同，不复制 producer engine。

当前既有 mmWave 预检口径仍是：44 registered / 39 loadable / 33 current analysis groups / 5 structural missing (036/038/040/041/047)。这不是本轮硬盘迁移的缺失项。

目标机完成毫米波标准化结果后，再按未来全量匿名 participant mapping 接入跨模态合并。

## 7. 未来约 119 场统一分析的正确顺序

1. **Behavior**：用最终冻结的 `codex/formal-analysis-v2-portable` Behavior 正式入口，把当前 44 场与新增约 75 场用同一版本重新生成标准化结果。
2. **Identity**：新旧所有约 119 场重新做全量匿名身份映射；不得追加旧 44 场 mapping。
3. **NIR**：当前 44 场从 `02_RITnet_latest` 重新生成正式下游表；新增场次按同一 pupil-only/topology/schema 生产并进入同一 staged pipeline。
4. **RGB**：当前 44 场先在本机形成统一标准化包；新增场次使用同一 schema/指标定义生成对应表。
5. **mmWave**：目标机按毫米波正式 producer 生成标准结果。
6. **Fusion**：所有模态统一使用 canonical `session_id` + participant cluster/repeat participant key，并按 unit（trial/probe/cycle/block/session/participant_group）合并。
7. **Inference/prediction**：重复测量、cluster、bootstrap 和 train/test folds 全部基于新生成的全量匿名 participant mapping；同一参与者任何 session 不得跨 train/test。
8. **报告**：只有通过失败门、模型准入、QC、provenance 与 unit 检查的结果才能进入正式报告；工程运行成功不等于科学效应或测量有效性。

## 8. 当前剩余任务清单（供新对话直接执行）

### P0：不要再制造分支混乱

- 不新建新的“总正式分析主线”。
- 继续使用 `kyandi233-dev/Attention-Analysis@codex/formal-analysis-v2-portable`。
- 开始任何修改前先 fetch 并记录该分支最新远端 HEAD；不能根据本文件记录的旧 HEAD 猜当前状态。
- `codex/nir-formal-v2-rejected-rework` 不得复活为正式路线。

### P1：统一 current 44-session identity adapter

- 读取实际 `anonymous_session_participant_mapping.csv` schema；
- 以 `session_id` + `anonymous_participant_group_id` 为当前正式身份联结来源；
- 映射到 formal v2 规范 participant cluster/repeat participant 字段；
- 不得从 90-session registry、RGB duplicate 表或历史 registry 替代当前 44 场正式 mapping；
- 如果没有 explicit visit order，不得推断；B1–B2 session 内分析继续可做，真正 visit-order 分析 fail closed。

### P2：Behavior 一键正式重建

- 核验/补齐 raw `E:\正式实验\sub-XXX_\beh` → 标准 trial/probe/cycle/block/session/participant-group → v3 inference/report 的统一 runner；
- 输出完整 schema/QC/failure/manifest/hash；
- 当前 44 场先小样本 smoke 再全量重跑；
- PR22 旧统计不直接冻结为正式结果。

### P3：NIR current topology → 正式 staged 结果

- 使用已迁移 `E:\_Analysis-needed\02_NIR\02_RITnet_latest`；
- 运行 pupil-only staged pipeline，不再用 PIR/旧虹膜/临时四图入口；
- 生成 Figure01–10、失败表、完整 provenance 和统一 Behavior 窗口/主键；
- 真实数据先最小 smoke，再 44 场。

### P4：RGB 44 场统一 derived/analysis-ready 层

- 在当前已配置 RGB 环境统一从 raw-first parquet 派生；
- 保证 44 场字段一致；
- 输出 coverage、failure、schema、trial/probe/cycle/block/session 层（适用时）与中文指标说明；
- 完成后再把标准化 RGB 包复制到 `E:\_Analysis-needed\03_RGB`。

### P5：迁移收口

- Behavior 原始数据不重复复制；
- 当前 44 场 identity mapping + manifest + SHA 保留；
- NIR current RITnet/latest 与历史 YOLO 分层保留；
- RGB raw-first + 新标准化结果保留；
- mmWave 本轮不迁；
- 最后生成迁移 inventory、modality status、source/provenance 说明和校验清单。

## 9. 本轮对话中必须保留的纠正记录

1. **Behavior raw 不重复迁移。** 原始 `beh` 已在同一块 E 盘，复制到 `_Analysis-needed` 是冗余方案，已撤回。
2. **`050-sart-formal` 不是预实验。** 它是旧正式 19 人版本，但不是当前 44-session 版本，因此只能作为 history。
3. **`01_current-derived` 不是本机已有目录。** 它曾只是迁移目标分类名；当前并没有已完成并冻结的最新 44-session v3 真实结果目录。
4. **PR22 有真实 44-session 派生表，但不是当前最终正式结果。** 可以审计/回归，不可把旧模型/图/报告直接冻结。
5. **不要把 RGB `rgb_duplicate_subjects.csv` 当重复被试身份表。** 当前 44 场正式身份映射是 `anonymous_session_participant_mapping.csv`。
6. **不要把 90-session repeat registry 当当前 44 场主映射。** 本轮最终冻结的是 `anonymous_participant_mapping_phase0_1_20260829` 下的 44-session mapping。
7. **不要把 `rejected` 分支误说成当前完整主线。** 当前完整正式下游默认基线始终是 `Attention-Analysis@codex/formal-analysis-v2-portable`；`codex/nir-formal-v2-rejected-rework` 继续保持 rejected/history。
8. **当前 `协作治理` 的错误反馈已经读过并通过 PR #28/#29 大量落实。** 剩余任务应是在 portable 上收口真实输入适配、一键 runner 和真实数据验证，不是重新发明另一条总路线。
9. **迁移命令都是 COPY。** 本轮没有授权删除、剪切或移动源数据。
10. **未来约 119 场必须重新做全量身份映射。** 当前 44 场 mapping 只用于当前批次，不能直接追加扩展。
