# 最终管线修改后代码验收（2026-08-30）

## 1. 核验对象

- 权威代码仓库：`kyandi233-dev/Attention-Analysis`
- 权威分支：`codex/formal-analysis-v2-portable`
- 修改前检查点：`7b473782ffd8faf3b137a152117227341181d8d8`
- 修改后检查点：`710c0964785527f25fb9733da4d70d4e4f1b0589`
- 干净分离提交工作树：`D:\AAAWORK\07-竞赛\厚璨杯\验证工作树\Attention-Analysis-formal-v2-portable`

本记录承接 [修改前审查](4.3-下一轮修复修改前完整审查_20260830.md) 与 [网页对话同步记录](4.4-最终管线与网页对话同步记录_20260830.md)。它只验收代码结构和定向合成测试，不把真实 44 场统计标记为完成。

## 2. 修改后实现映射

| 要求 | 修改后实现 | 当前验收边界 |
|---|---|---|
| 场次主表与问卷左连接 | 新增 `identity_questionnaire.py`（身份与问卷适配器），保留缺问卷场次并审计 identity/registry 一致性 | 真实 44 场仍需核对联结覆盖、冲突和缺失状态 |
| 重复参与者分组 | 将 `participant_key` 接入身份、访问顺序和参与者互斥逻辑 | 真实分折必须再次检查同一参与者不跨训练/测试侧 |
| Go 遗漏机制区分 | 新增 `behavior_error_taxonomy.py`（行为错误分类），并向多个行为尺度传播子类型 | 子类型先作为候选质量控制；任务语义未复核前不把某类独占解释为注意失败 |
| 访问顺序 | 新增经 registry 核验的访问顺序敏感性分析 | 缺少核验顺序时保持不可评估，不从场次编号猜顺序 |
| 行为图表 | 增加指标图族、图内标题阻断、外置题注和覆盖审计 | 必须真实运行并逐张打开图片；测试不能代替视觉检查 |
| NIR 三类参考值 | 新增 `baseline_contract.py`（参考值语义合同），分离场次中心化、事件前局部基线和静息基线 | 静息基线仍未放行；需要协议、光照、事件和有效覆盖证据 |
| NIR 调整与重复访问 | 新增调整效应图、身份审计和访问顺序敏感性 | 只有真实视觉协变量进入时才能称为亮度/视觉调整后模型 |

## 3. 本地定向测试

- 解释器：`D:\Code\python\python.exe`
- 工作树提交：`710c0964785527f25fb9733da4d70d4e4f1b0589`
- 命令：`python -m pytest -q tests/test_formal_identity_questionnaire.py tests/test_behavior_error_taxonomy.py tests/test_behavior_visit_sensitivity.py tests/test_behavior_figure_contract_complete.py tests/test_nir_baseline_contract.py`
- 结果：5 个测试文件，共 12 项测试通过。
- 非阻断提示：`requests` 报告 `urllib3/chardet/charset_normalizer` 版本组合警告；本次测试未失败，但正式环境清单应记录实际依赖版本，避免换机时产生差异。

本轮没有运行完整测试集，没有接触行级真实身份数据，没有运行代表性真实场次，也没有生成 44 场统计结果。

## 4. 下一道放行门

1. 专门 Git 同步任务把 `710c096` 和本证据提交冻结到执行任务包。
2. 本地执行任务只能使用权威脚本、配置和 manifest，不得另写临时分析脚本。
3. 先核验本机路径注册表、问卷身份输入、44 场 NIR 唯一来源和行为原始输入。
4. 运行完整相关测试集，再用 1–2 个场次做隔离式真实试跑。
5. 核对身份左连接、遗漏分区恒等式、NIR 视觉协变量、失败表、图片无标题、题注表和图表覆盖审计。
6. 小样例通过后才扩大到 44 场；全量产物必须包含 manifest、字段结构、校验值、失败表、图表与中文结果说明。

## 5. 当前状态

代码修复已经从“计划/修改前审查”进入“修改后合成测试通过、等待真实数据验收”。行为、NIR 或跨模态的正式科学结果仍未因此自动成立。
