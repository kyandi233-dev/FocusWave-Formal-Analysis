# Behavior 与 NIR 修复状态校正（2026-08-29）

本文件补充并校正 `4.1-Behavior与NIR科学分析完整性修复续记录_20260829.md` 建立后的后续提交状态。

## 当前正式代码 HEAD

`kyandi233-dev/Attention-Analysis@codex/formal-analysis-v2-portable`

当前核验 HEAD：

`7b473782ffd8faf3b137a152117227341181d8d8`

## adjustment audit 已完成 runner 接入

`4.1` 中“adjustment_audit 仍需挂入统一 runner 并补 contract test”这一项在随后提交中已经完成：

- `scripts/nir_formal_pipeline.py` 已调用 `run_adjustment_audit`；
- `scripts/nir_build_analysis_tables.py` 已调用 `run_adjustment_audit`；
- 新增 `tests/test_nir_adjustment_audit.py`；
- 若 adjustment audit 为 `not_estimable`，正式入口返回非 0，不允许把缺失视觉审计静默当成完成。

因此当前该项不再属于代码层阻塞。

## 当前仍未解除的正式运行阻塞

1. 新问卷 `participant_key / visit_order` 尚未接入 authoritative identity adapter；
2. 现有 44-session `cohort_manifest` 仍需与新 `questionnaire_derived_data.csv / subject_repeat_registry.csv` 做真实 overlap / grouping parity 审计；
3. questionnaire left join / `questionnaire_present` / `sub-102_` 缺失问卷 contract 尚未接入代码；
4. questionnaire variable-role registry 尚未冻结；
5. GitHub 仍没有 CI pass evidence；
6. 尚未完成本地 pytest 与 representative real-session smoke；
7. pupil / Behavior candidate endpoint 尚未经过真实数据 scientific freeze。

所以当前允许拉取代码运行 tests / smoke，但仍不把 44-session 正式统计解释标记为 release-ready。