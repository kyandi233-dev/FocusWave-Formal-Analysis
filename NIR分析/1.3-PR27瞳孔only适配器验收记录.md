# PR27 瞳孔-only 适配器验收记录

## 审查范围

审查仓库 `Attention-Analysis` 的分支 `codex/nir-pupil-only-adapter-v1`，头提交为
`f89ff57d4ddfa5c375174ee64148c10f8cc297e1`。Git 合并引用核验的目标基线为
`b2a96c0df6e6db88c599332815fb24cc93c07fe0`，对应 `analysis/multimodal-integration`。
审查在独立临时 worktree 完成，未切换或污染用户现有工作树，未合并、未推送。

## 实际核验

- 适配器只投影 pupil 几何；`hard_iris_fraction` 和 `soft_iris_fraction` 保留为分割字段，不能作为虹膜几何。
- `refuse_pir_without_iris_geometry()` 对 PIR/OAR 直接抛出 `IrisGeometryUnavailableError`，输出拒绝 PIR/OAR 比值字段。
- 来源 manifest 支持 schema 6/7；按字段名映射，核验来源版本、subject、必需字段、复合主键、眼别和 `unix_ms` 单调性。
- 行为连接使用 `unix_ms` 与 `absolute_onset_time`；当前/上一刺激、相对亮度、对比度及失败原因均有记录。
- source missing、RITnet missing、ROI clipping、几何无效、时间异常和 interpolation-only 分轨。

使用 `D:\Code\python\python.exe`（Python 3.13.0）运行 `tests/test_nir_pupil_only_adapter.py`，结果为 **12 passed**。
CI 配置明确新增该定向测试，但本次无法从 PR 网页/API 获取独立 CI 状态；没有把网页 404 当作通过。

## 验收结论与复跑门

**代码验收通过，可允许本地最小复跑。** 复跑落点为
`codex/nir-pupil-only-adapter-v1@f89ff57d4ddfa5c375174ee64148c10f8cc297e1`，必须使用已核验的真实来源 manifest 和新输出根。

**正式科学准入未通过。** 当前证据不等价于 27 场或 44 场正式运行，不得把 pupil-only 适配器通过写成 PIR/OAR 或生理效度结论。

