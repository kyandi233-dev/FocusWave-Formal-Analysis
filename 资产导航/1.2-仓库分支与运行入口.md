# 仓库、分支与运行入口

> 本文件服务于阶段 0 资产冻结和任务交接。代码仓库只读核验，不在本任务中修改；本任务允许写入的文件仅为正式分析仓库的资产导航和既有工作记录。

## 2026-08-30 当前入口覆盖说明

本文件下方“一、Git 基线”至“五、交接与禁止事项”保留 2026-08-29 阶段 0 的现场快照，不回头改写历史。自 2026-08-30 起，涉及正式下游行为、NIR、RGB 标准结果接入和跨模态合并时，优先使用以下入口：

| 职责 | 权威仓库与分支 | 本次核验提交 | 本地核验位置 | 当前含义 |
|---|---|---|---|---|
| 正式下游代码主线 | `kyandi233-dev/Attention-Analysis@codex/formal-analysis-v2-portable` | `710c0964785527f25fb9733da4d70d4e4f1b0589` | `D:\AAAWORK\07-竞赛\厚璨杯\验证工作树\Attention-Analysis-formal-v2-portable`（分离提交工作树） | Behavior + NIR + RGB 外部结果接入 + 毫米波外部生产器接入 + 跨模态合并的唯一默认基线；`7b47378` 为修改前检查点 |
| 分析设计、修复台账与报告草稿 | `kyandi233-dev/FocusWave-Formal-Analysis@codex/code-fix-ledger` | `e71e1415d6af5be5d903c79541cd0024fd0156f7`（本次修改后验收记录写入前） | `D:\AAAWORK\07-竞赛\厚璨杯\验证工作树\FocusWave-Formal-Analysis-正确台账` | 记录计划、代码提交、测试/真实运行证据、结果准入和报告章节；不复制源码或行级真实数据 |
| NIR/RGB 上游生产与历史实现 | `Attention-Analysis` 的 AMD、NVIDIA、恢复、基准和历史分支 | 运行前逐项核验 | 各既有工作树 | 继续保存生产运行时、恢复修复和独立基准资产；不能替代正式下游主线，也不能因主线收口而批量删除 |
| 毫米波生产器 | `greenboo26/focuswave-multimodal-attention-analysis` 对应正式分支 | 由另一台正式分析电脑现场冻结 | 不在本轮迁移包内 | 本仓库只接收标准结果与缺失清单，不复制毫米波信号生产引擎 |

正式下游主线的当前入口是 `scripts/formal_multimodal_analysis.py`、`scripts/sart_formal_analysis_v2.py`、`scripts/nir_materialize_analysis_ready.py`、`scripts/nir_build_analysis_tables.py`、`scripts/nir_formal_pipeline.py` 和 `scripts/nir_validate_pupil_formal.py`；主要配置为 `configs/formal_multimodal_v2.yaml`、`configs/behavior_formal_v2.yaml`、`configs/paths.example.yaml`、`configs/cohort_manifest.example.csv` 和 `configs/nir_source_manifest.example.csv`。机器盘符只写入本地路径注册表，不能进入可移植科学配置。

当前必须区分三种完成状态：上游生产目录可见 44 个 NIR 场次，不等于这些场次已经由当前主线完成统一物化；`710c096` 的新增契约与 12 项定向合成测试通过，不等于完整测试集和真实场次试跑通过；真实场次试跑通过，也不等于 44 场正式统计和报告准入完成。

## 一、Git 基线

正式分析证据库本身已现场核验为：

| 项目 | 现场值 |
|---|---|
| 路径 | `D:\AAAWORK\07-竞赛\厚璨杯\040-竞赛报告与多模态分析\正式分析完整管线与报告证据库` |
| 分支 | `main` |
| HEAD | `ad11f1df1f9e8847d9ecc4a97314faa589692a9e`（`ad11f1d`） |
| 远端 | `https://github.com/kyandi233-dev/FocusWave-Formal-Analysis.git` |
| 远端基线 | `origin/main` 与本地 HEAD 相同 |
| 工作树 | 开始核验时 clean（干净）；本任务只新增/更新资产导航和 `运行记录与证据/08-29-02-第一阶段资产与样本冻结.md` |

本轮只对正式分析证据库执行了 `git fetch --no-tags origin main` 以满足基线核验；没有执行 `pull`、切分支、commit 或 push。其他代码仓库均未执行 pull、commit、push 或切分支。

## 二、关联代码仓库状态

下表来自 2026-08-29 的现场命令 `git status --short --branch`、`git branch -vv`、`git log -1`、`git remote -v` 和 `git worktree list`。dirty（脏）表示存在未提交修改或未跟踪文件；它不是本任务产生的变化，也不能被清理。

| 权威仓库 | 分支 / HEAD | 远端 | 工作树现场状态 | 权威职责 |
|---|---|---|---|---|
| `D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis` | `nir-seven-algorithm-benchmark` / `b612da397e9ad495722faae34dc20bc34ecf6e5c`（`b612da3`） | `kyandi233-dev/Attention-Analysis.git` | dirty；有已修改 docs 与 pupil benchmark 代码 | NIR 七算法 benchmark、历史 NIR/RGB 主代码与部分文档；本任务只读 |
| `D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-analysis-validation` | `analysis/multimodal-integration` / `b2a96c0df6e6db88c599332815fb24cc93c07fe0`（`b2a96c0`） | `kyandi233-dev/Attention-Analysis.git` | dirty；有修改和未跟踪重复 registry / 融合验证文件 | NIR/RGB validation（验证）、重复参与者 registry（注册表）和旧融合 smoke（快速验证） |
| `D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-amd-DirectML` | `amd-DirectML` / `dd56f7fc9faccee4d06bb356e9d2df91088f38d3`（`dd56f7f`） | `kyandi233-dev/Attention-Analysis.git` | clean；本任务未修改 | 当前 AMD DirectML NIR `fullclass-final` producer（生产器）、runtime（运行时）和正式配置 |
| `D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-rgb-amd` | `rgb-amd` / `eea94e87540f226d6005f87667d453291ca96b19`（`eea94e8`） | `kyandi233-dev/Attention-Analysis.git` | dirty；相对 `origin/rgb-amd` 落后 5 个提交，并有未跟踪文件 | RGB AMD 提取和相关运行资产；本阶段不直接运行 |
| `D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\focuswave-central` | `codex/mmwave-formal-reanalysis-v2` / `d87229afe071f23450728a6d617ec82317e6c9df`（`d87229a`） | `greenboo26/focuswave-multimodal-attention-analysis.git` | clean；本任务未修改 | 行为、毫米波、canonical（标准）launcher（启动器）、机器包契约和中央合并逻辑 |
| `D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\Attention-Analysis-0816-history` | `nvidia-cuda` / `14d33a76a53806c5d5366cd46e06b7f8660a121b`（`14d33a7`） | `kyandi233-dev/Attention-Analysis.git` | clean；本任务未修改 | 预实验截断二/BBB-v3.0 历史行为分析；仅复用方法和字段，不搬用数值结果 |

关联仓库中的 dirty、behind 或未跟踪状态均属于后续专门 Git 任务和对应代码任务的输入；本任务不尝试修复。

## 三、实际运行入口与配置

本阶段没有启动正式统计、全量行为分析、NIR 重跑、毫米波批量分析或融合分析。以下是已核验的入口，供后续任务使用；运行前必须重新检查分支、工作树、配置摘要、解释器和输入 manifest。

### 1. NIR 当前正式 producer

- 权威仓库：`Attention-Analysis-amd-DirectML`，当前已核验 commit `dd56f7f`。
- 配置：`runtime\nir-formal\config.yaml`。实际源根包含 `E:/正式实验`；正式边界是 `min_subject_number: 31`，明确排除 `sub-9504`；RITnet 输入为 640×400，batch size（批大小）为 16，输出目录名为 `ritnet-fullclass-final`。
- 正式用户入口：`runtime\nir-formal\run_ritnet_fullclass_extension.py`（单个历史 formal run）和 `runtime\nir-formal\run_ritnet_fullclass_batch.py`（批量入口）。
- 完成判据：每场必须有通过严格校验的 `completion.json`、`manifest.json`、`summary.json`、`data\eye_metrics.csv`、`data\frame_coverage.csv` 和 bounded QC（有界质量控制）证据；只有文件存在不代表完成。
- 当前数据根：`D:\_AttentionData\Beijing-NIR\amd-directml`。其中 `final-topology`、`historical-yolo\ritnet-fullclass-final`、`validation\pupil-geometry` 和兼容 junction（连接目录）不能未经来源选择直接混合。

### 2. 行为正式 producer

- 当前代码入口：`Attention-Analysis-amd-DirectML\scripts\sart_formal_analysis.py`。
- 当前配置：`configs\behavior_formal.yaml`；原始根为 `E:/正式实验`，行为正式输出配置根为 `D:/_AttentionData/Beijing-Behavior/formal-v1`。
- 历史兼容入口：`scripts\sart_formal_analysis.py --stage extract|metrics|stats|figures|report|all`。本阶段只审计配置和原始文件存在性，未执行任何 stage（阶段）。
- `configs\sart_bbb_v3_0.yaml` 和 `docs\030-behavior\history\BBB-v3.0` 是历史 BBB-v3.0 路线；不得将其已有结果当作当前 44 场次正式结果。

### 3. RGB producer

- 当前代码入口候选：`Attention-Analysis-amd-DirectML\scripts\rgb_analysis.py` 及 `scripts\rgb_formal_motion_pose.py`。
- 当前配置：`configs\rgb_analysis.yaml`，明确 `min_subject_number: 31` 并将 `sub-9504` 标记为不属于正式分析 cohort（样本组）。
- 已有工程输出：`D:\_AttentionData\Beijing-RGB`；`cohort_manifest.json` 记录 44 selected、44 complete_or_skipped、0 failed。主脸、眼睑、眨眼和质量规则尚未在本阶段冻结。

### 4. 行为/毫米波中央入口

- 权威仓库：`focuswave-central`，当前分支为 `codex/mmwave-formal-reanalysis-v2`，HEAD `d87229a`。
- 事实审计入口：`scripts\canonical\audit_local_analysis_library.py`；路径检查入口：`scripts\path_registry.py --check`。
- 标准本地调度入口：`scripts\canonical\run_local_analysis.py`；其 `--list` 可查看登记的 analysis（分析）ID。该入口当前登记了历史/候选 producer，不能把“已登记”解释为本轮 44 场次已运行。
- 毫米波候选 producer：`pipelines\mmwave\run_beijing_sensor_increment_v1.py`、`pipelines\mmwave\frozen_c1_metrics.py`、`scripts\process_vital_signs_v*.py` 等；实际使用前须为 `.npz` 与 `.bin` 两种原始存储建立同一字段契约，并处理 `sub-047` 缺失。

## 四、解释器记录

本阶段未运行 producer，因此没有新的 `sys.executable`（实际 Python 解释器）记录。项目已核验的固定入口为：

- 主分析：`D:\Code\python\python.exe`；44 场次旧 NIR/行为 inventory 曾记录此解释器，但该历史运行不等于本轮正式运行。
- 当前后台/正式 NIR runtime 观察到的解释器：`D:\CondaEnvs\nir-amd\python.exe`；后续 NIR 运行需现场打印 `sys.executable` 并写入运行记录。
- pupil-detectors 与 PyPupilEXT：`D:\AAAWORK\07-竞赛\厚璨杯\venv-pupil\Scripts\python.exe`；只用于七算法 benchmark，不作为当前 `fullclass-final` 生产器解释器。

## 五、交接与禁止事项

允许下一步任务做的事：在已解决 NIR 来源冲突、重复身份缺口和毫米波 `sub-047` 缺失状态后，基于明确的代码 commit 和配置生成新的输入 manifest，并先做一个场次的小样例审计。

当前禁止做的事：在 dirty 代码仓库直接切分支或清理；用旧 v1.2 PIR 表替代 current `fullclass-final`；将 `algorithm_returned`（算法返回）当作质量通过；把场次当作独立参与者；在样本/来源未放行前启动正式统计或 44 场次大规模重跑；由本任务自行 commit、push 或 pull。
