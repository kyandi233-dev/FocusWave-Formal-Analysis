# AGENTS.md｜正式分析仓库自动入口

本文件只规定 Codex 进入本仓库时的稳定读取顺序和角色门控。项目现状以根目录 [README.md](README.md) 为入口，协作职责与 Git 治理以 [运行记录与证据/1.2-协作角色、任务分工与Git治理.md](运行记录与证据/1.2-协作角色、任务分工与Git治理.md) 为唯一权威；本文件不重复维护正文规则。

## 稳定读取顺序

1. 读取根目录 [README.md](README.md)，确认本仓库当前阶段、证据边界和各模块入口。
2. 读取目标模块的 `README.md`；若任务涉及运行记录，继续读取 [运行记录与证据/README.md](运行记录与证据/README.md)。
3. 按任务需要读取 [运行记录与证据/1-跨电脑复现与移植交付规范.md](运行记录与证据/1-跨电脑复现与移植交付规范.md)、[运行记录与证据/1.1-人工智能任务接手说明.md](运行记录与证据/1.1-人工智能任务接手说明.md) 和 [运行记录与证据/1.2-协作角色、任务分工与Git治理.md](运行记录与证据/1.2-协作角色、任务分工与Git治理.md)。
4. 读取当前模块最近的工作记录、产物索引、字段结构和配置；不要以旧报告、旧对话或目录名替代现场核验。
5. 若任务要修改代码，转到对应权威代码仓库，读取该仓库的入口规则、当前分支、配置和测试说明；本仓库只登记代码提交、运行记录和产物索引。

## 新文件命名规则

- 每个目录的入口固定为不编号的 `README.md`。
- 同一目录中的新说明文件依次使用 `1-标题`、`2-标题`、`3-标题`；同主题或从属内容使用 `1.1-标题`、`1.1.1-标题`。每个目录内部重新从 `1` 开始，不继承父目录或模块编号。
- 需要在目录最前面显示的新文件使用 `_1-标题`、`_2-标题`，不得再用 `000`、`00`、`01`、`02` 等补零编号置顶。
- 程序、配置、模型、数据与测试文件默认不编号，除非确有执行顺序。既有补零文件属于历史溯源，不在无明确授权时批量重命名。
- 新工作记录使用 `MM-DD-N-标题.md`，同一天按 `1`、`2`、`3` 接续，不补零。

## 角色与 Git 门控

- 开始任何任务前，按 `1.2` 识别任务角色、允许的仓库和交付对象；角色边界不清时先停止分派，不擅自扩大权限。
- 多任务协作固定采用“监督任务直接与网页版完整管线对话逐问澄清 → 专门 Git 同步任务冻结远端分支和完整提交 → 具体执行任务接收完整任务包”的顺序。监督任务不得把尚未问清的问题直接转交本地子任务，也不让本地子任务代为操控网页版。只有任务包写清仓库、分支、提交、要求、禁止事项和逐项验收后，才允许进入具体执行；执行任务不得自行发明临时脚本或替代入口。
- 任何仓库任务开始前都要核验实际路径、分支、HEAD、远端和工作树，并执行安全 `fetch`；发现远端新增提交、未预期本地修改或冲突时停止提交与合并。
- 不删除、重命名、覆盖用户既有文件，不 `reset` 用户工作，不强制推送；网页 GPT 的代码修改必须走独立分支和合并请求（PR，代码审查请求）。
- 本地数据执行任务完成后不自行 `pull`、`push` 或提交；由专门 Git 任务统一核验、记录、提交和推送。
