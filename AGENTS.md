# AGENTS.md｜正式分析仓库自动入口

先读根目录 `AI_PROJECT.md`。中央跨 AI 治理唯一来源是 `greenboo26/ai-governance@main`；本仓库规则只能在中央 hard rule（硬规则）边界内做项目细化，不能建立第二套全局治理。

项目现状以根目录 [README.md](README.md) 为入口；协作职责与 Git 约束继续由 [运行记录与证据/1.2-协作角色、任务分工与Git治理.md](运行记录与证据/1.2-协作角色、任务分工与Git治理.md) 记录，但若其中历史规则与当前中央 `rules/repository-sync.yaml`、`rules/execution.yaml` 或 `rules/durable-project-record.yaml` 冲突，以当前中央硬规则为准，并记录冲突来源与影响。

## 稳定读取顺序

1. 读取 `AI_PROJECT.md` 和根目录 [README.md](README.md)，确认仓库角色、当前阶段、证据边界和各模块入口。
2. 读取目标模块的 `README.md`；涉及运行记录时继续读取 [运行记录与证据/README.md](运行记录与证据/README.md)。
3. 按任务需要读取 [运行记录与证据/1-跨电脑复现与移植交付规范.md](运行记录与证据/1-跨电脑复现与移植交付规范.md)、[运行记录与证据/1.1-人工智能任务接手说明.md](运行记录与证据/1.1-人工智能任务接手说明.md) 和 [运行记录与证据/1.2-协作角色、任务分工与Git治理.md](运行记录与证据/1.2-协作角色、任务分工与Git治理.md)。
4. 读取当前模块最近的工作记录、产物索引、字段结构和配置；不要用旧报告、旧对话、目录名或记忆代替当前仓库核验。
5. 若任务需要修改 producer（生产代码），转到拥有该代码的权威仓库读取其入口规则、当前分支、配置和测试；本仓库只登记相应提交、运行记录、研究决策和产物索引。

## 新文件与结果组织

- 每个目录的入口固定为不编号的 `README.md`；现有历史命名保留，不为整齐而批量重命名。
- 同一目录的新说明文件按该目录既有约定编号；新工作记录继续使用 `MM-DD-N-标题.md`。
- 不新造平行的状态体系、临时结果根目录或重复的“当前方法”文档。结果应登记到已有运行记录/结果索引；大型、本机或敏感结果只记录可复现路径/manifest（运行清单）和 Git-safe 汇总。
- 任何材料性分析、结果、bug、方法决定或重排必须遵守中央 `rules/durable-project-record.yaml`：记录问题、数据/样本/窗口/时间语义、代码/提交/入口、输入输出、关键数字、比较基线、判断依据、状态、未解决问题、验证和下一依赖，并从高可见入口链接。

## 角色、Git 与执行门控

- 开始任何任务前识别任务角色、权威仓库、当前分支/HEAD、输入、输出和验收标准。角色边界确实无法从当前仓库和任务上下文解决时才阻塞，不把普通可解析信息反复抛回用户。
- 多 AI 可以并行做只读检索、独立审计或互不重叠的工作；同一任务对同一文件集合应保持一个明确写入者，避免并发覆盖。
- 使用已经存在且获得授权的分支。**不得因为“网页 GPT”“Codex”“实验”“整理文件”等理由自动创建分支或强制要求 PR（代码审查请求）**；只有用户或项目任务明确要求，或已有项目证据明确指定时才创建/使用新分支或 PR。
- 任何 Git 写入前核验 repository、branch、HEAD、remote 和可见工作树状态；保留未预期的用户修改与未跟踪证据。不删除、重命名、覆盖用户文件，不 `reset`、`clean`、强制推送。
- 在当前任务已明确授权 GitHub 写入时，可由当前执行 AI 完成与任务直接相关的非破坏性提交、状态记录和验证；不再强制把所有提交拆给“专门 Git 任务”。高风险、破坏性、跨授权范围操作仍需按中央规则重新授权。
- 用户明确要求执行时，应把已授权且必要的工作推进到可验证结果，不停在计划、局部修复或“发现问题”。发现材料性问题必须给稳定标识/记录，写明影响、根因置信度、修复状态和复测证据，或明确 `BLOCKED`/延期理由。
- 已知有效上游组件缺失或损坏时，先恢复/修复并验证，再做最终下游窗口、参数或模型比较；若提前比较只能作为诊断，必须标明 `DIAGNOSTIC_ONLY`（仅诊断）。
- 若 skill、项目文件或其他指令导致任务暂停、改路线或额外审批，报告具体文件/规则及其实际影响，不静默改变方案。
