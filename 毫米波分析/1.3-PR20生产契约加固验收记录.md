# PR20 毫米波生产契约加固验收记录

## 审查范围

审查仓库 `focuswave-multimodal-attention-analysis` 的分支 `codex/mmwave-production-contract-hardening`，头提交为
`fc682b491fcbeeb9bd1b030c8af9da33282d2846`。目标基线为用户指定并经 Git 核验的
`codex/mmwave-formal-reanalysis-v2@d87229afe071f23450728a6d617ec82317e6c9df`。
审查在独立临时 worktree 完成，未切换或污染用户现有工作树，未合并、未推送。

## 实际核验

- 生产入口接受外部冻结 JSON/CSV manifest，原样透传 session、匿名组和重复参与者字段，不硬编码 44、39、38 或 33。
- 32 字节 `.bin` 加 0 字节 timestamps、空毫米波目录和无可分析 NPZ 按文件事实拒绝。
- 严格 JSON 递归把非有限值变为 `null`，使用 `allow_nan=false`；非空输出目录和空记录集均拒绝。
- VMD 只接受直接依赖 `vmdpy==0.2`；缺依赖、版本不符或符号不可导入时阻断，不自动切换 BP。
- 全 NaN 候选拒绝，不回落到 `ch0/bin10`，并记录 `algorithm_returned=false`、`quality_valid=false` 和拒绝原因。
- 单段字段统一为 `breath_rate`，并保留计算、工程质量、外部验证、行为关联和报告准入状态。

使用 `D:\Code\python\python.exe`（Python 3.13.0）运行定向测试，结果为 **11 passed**。VMD 不读数据预检为
`blocked/MISSING_VMDPY_DEPENDENCY`、`selected_method=null`、退出码 2；显式 `bp_heart` 预检为 `pass`、退出码 0。

## 验收结论与复跑门

**代码验收通过，可允许本地最小复跑，但 VMD 受依赖门控。** VMD 复跑落点为
`codex/mmwave-production-contract-hardening@fc682b491fcbeeb9bd1b030c8af9da33282d2846`；若只做工程冒烟，必须显式使用 `--method bp_heart`。

**正式科学准入未通过。** 需先完成至少一个冻结 manifest 场次的新输出根复跑，再由负责人批准 39 场/33 组增量生产；没有 ECG/RSP 外部参考时，不得声称外部生理验证通过。

