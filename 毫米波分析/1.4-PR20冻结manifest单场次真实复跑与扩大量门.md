# PR20冻结manifest单场次真实复跑与扩大量门

> 复跑日期：2026-08-29  
> PR20工作树：`D:\AAAWORK\07-竞赛\厚璨杯\验证工作树\毫米波-PR20-fc682b4`  
> 头提交：`fc682b491fcbeeb9bd1b030c8af9da33282d2846`，detached、干净  
> 结论：代码级契约通过；单场次数据复跑未通过扩大门；不启动39场/33组正式生产。

## 1. 复跑边界和冻结输入

本轮使用冻结的当前队列匿名映射源文件 `D:\_AttentionData\anonymous_participant_mapping_phase0_1_20260829\anonymous_session_participant_mapping.csv`，从中只抽取`sub-031`一行作为本次单场次输入清单，不修改冻结源文件。源文件包含44场、38个匿名分析组，其中6个双场重复组；该场次的具体匿名组键仅保留在数据区输入中，不写入 Git 文档。

历史单场次manifest为 `D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_frozen_manifest.csv`，SHA-256为`875464A7D5D2610559AB88D2E988FC701EEC992269AFF32D4AD582C5B7224DA9`。它记录了旧阶段语义`identity_status=provisional_registry_history`、`evidence_source_code=R2+Q3`，作为本次已完成试跑的不可回写历史输入保留，不再作为阶段0.2身份语义的当前manifest。阶段0.2修正版manifest另存为`D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_stage0_2_manifest.csv`，SHA-256为`8408C14005885AF8E6955DF43A5B2887F8C8CD3BD26362F8DD0BDDAF1B8C9B63`，并改为`identity_status=frozen_current_cohort_anonymous_group`、`evidence_source_code=E2_primary+Q3_independent;R2_sensitivity_only`。本轮只生成并核验该manifest，没有用它重新运行算法；具体匿名组键不在本 Git 文档展开。

阶段0.2身份口径为当前队列44个session、38个匿名分析组、6个双场重复组；E2姓名主匹配并以手机号/学号交叉核验，Q3匿名键独立复核；旧R2 corrected registry只覆盖31/44，仅作敏感性辅助。13个R2外场次是当前队列单场分析组，不是身份未知。毫米波仍为39/44可加载、对应33个当前队列分析组；`global`跨队列身份尚未冻结，未来约72场次接入时必须全量重建映射。

当前输入事实只读复核结果：39个场次可加载、联结后为33个当前队列匿名分析组；`sub-036`、`sub-038`、`sub-040`、`sub-041`的`.bin`均为32字节且timestamps均为0字节，是无效占位；`sub-047`毫米波目录为空。5个场次不属于可加载原始数据，不进入本次复跑。

## 2. 解释器和方法预检

指定解释器为 `D:\Code\python\python.exe`，实测Python版本为3.13.0，`vmdpy`未安装。

`vmd_heart`预检结果：`status=blocked`、`failure_reason=MISSING_VMDPY_DEPENDENCY`、`selected_method=null`、退出码2；要求版本为`vmdpy==0.2`。本轮未安装依赖，未自动切换方法，保持VMD阻断。

显式`bp_heart`预检结果：`status=pass`、`selected_method=bp_heart`、后端为`scipy_bandpass`、`fallback_used=false`、退出码0。`bp_heart`仅作为工程冒烟方法，不替代VMD生产方法，也不改变科学方法定义。

## 3. 实际复跑命令

输出目录在运行前确认不存在：

`D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_bpheart_fc682b4`

实际命令为：

```powershell
$env:PYTHONIOENCODING='utf-8'; $env:PYTHONUTF8='1'
& 'D:\Code\python\python.exe' -u scripts/run_timeline_gated_mmwave_quality.py --roots 'E:\正式实验' --input-manifest 'D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_frozen_manifest.csv' --output-dir 'D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_bpheart_fc682b4' --run-analysis --method bp_heart --analysis-id mmwave_pr20_sub031_smoke_v1
```

进程退出码为0，但程序结果明确为`status=completed_with_failures`，不是整体成功：1个场次、2个分段成功、1个分段失败。

## 4. 分段结果和状态字段

输出摘要：`schema_version=mmwave_segment_analysis_v1`，`pipeline_version=v3.1.1-contract.1`，`analysis_id=mmwave_pr20_sub031_smoke_v1`，`n_sessions=1`，`n_segments_succeeded=2`，`n_segments_failed=1`。

| 分段 | `algorithm_returned` | `quality_valid` | `selection_status` | `failure_reason` | 观察到的结果 |
|---|---|---|---|---|---|
| baseline | `true` | `true` | `selected` | `null` | HR融合82.2 bpm；BR时域19.4 bpm；HR信号39/39窗口可用，硬门通过 |
| block1 | `false` | `false` | `rejected` | `NO_VALID_CHANNEL_BIN_SELECTION` | 无HR/RR/IBI/HRV数值输出；保留8个候选摘要，未回退到`ch0/bin10` |
| block2 | `true` | `true` | `selected` | `null` | HR融合81.2 bpm；BR时域18.9 bpm；HR信号113/113窗口可用，硬门通过 |

失败段由PR20逻辑显式记录为拒绝，不被进程退出码0掩盖。该结果证明代码级“全NaN候选拒绝、状态字段落盘、失败原因透传”行为生效；同时也证明当前单场次数据还不能通过扩大运行门。

## 5. 输出契约核验

### 5.1 严格JSON

输出目录共有5个JSON文件，5个均通过严格解析；递归扫描原始JSON字节未发现`NaN`、`Infinity`或`-Infinity`。PR20的有限数值转换和`allow_nan=false`行为在真实输出上通过。

### 5.2 同源HR/RR质量

baseline和block2的segment结果分别与对应生产JSON核对：`breath_rate`完全一致，`heart_rate.time_course.signal_quality`完全一致，`heart_rate.time_course.metrics`完全一致。质量字段不再从错误的`breathing_rate`或不存在的顶层`quality`读取。

### 5.3 状态字段

每个segment和`segment_analysis_rows.csv`均保留：

- `algorithm_returned`
- `quality_valid`
- `selection_status`
- `failure_reason`
- `hr_engineering_status`
- `br_engineering_status`
- `ibi_hrv_engineering_status`
- `external_validation_status`
- `behavior_link_status`
- `formal_report_status`

成功段的HR工程状态为`signal_gate_passed`，RR/BR为`computed_pending_engineering_gate`，IBI/HRV为`candidate_only`；失败段三类工程状态均为`rejected`。三段的外部验证仍为`not_available`，行为关联仍阻断，正式报告状态仍为`blocked`。

### 5.4 匿名键透传

既有匿名组键从冻结manifest透传至`crop_manifest.json`、`segment_analysis_summary.json`的记录和分段、`segment_analysis_rows.csv`三行；具体值仅保留在数据区产物中，`repeat_participant_id`保持空值，没有创建新的global身份映射。

### 5.5 分段表、manifest和哈希

`crop_summary.json`和`segment_analysis_summary.json`均保留输入manifest路径、SHA-256和`n_sessions=1`；`segment_analysis_rows.csv`为3行、34列，对应baseline、block1、block2。关键输出哈希为：

| 文件 | 大小 | SHA-256 |
|---|---:|---|
| `crop_manifest.json` | 2,468 B | `1FB4AFF58265281C0C82370969DB6CCC3BBEA3A2828002407C1A8812551E984E` |
| `crop_summary.json` | 874 B | `6CFF924EA93C799E4D7A6AF27BF02A9C8B9718904CF115F1B0971A83F612966F` |
| `segment_analysis_summary.json` | 1,471,189 B | `F0809B8AE3F194400A3E666178DE4B689C5BC2302EF09F837CD586A00D9F9FC5` |
| `segment_analysis_rows.csv` | 1,549 B | `3AE6E2F382A06B7BA919721436CD78F60DFD90B09F840438E849870066F97D0A` |

### 5.6 非空输出保护

在已经存在的上述输出目录上再次调用同一入口但不运行算法，程序返回`FileExistsError: OUTPUT_DIRECTORY_NOT_EMPTY`，退出码1；原目录仍存在且文件数未改变。非空输出保护实际生效，没有覆盖本次产物。

## 6. 代码和输入验收结果

PR20定向测试使用指定解释器运行：

```powershell
D:\Code\python\python.exe -m pytest -q tests/test_mmwave_production_contract_v1.py tests/test_mmwave_benchmark_contract_v1.py
```

结果为`18 passed in 1.55s`。仅有requests依赖版本提示，不影响测试退出码。中央工作树仍为PR20 detached、干净；本轮没有修改代码、测试或配置。

当前数据层复核为：冻结映射44行、38组，其中6组各含2个场次；可加载毫米波39场、33组；无效/空输入为`036/038/040/041/047`。这些统计均为当前队列口径，`global`跨队列身份仍未冻结；未来+72场次必须重新生成全量映射和合并键。

## 7. HR、RR/BR、IBI/HRV分层结论

| 指标 | 可计算 | 工程质量 | 外部生理验证 | 行为关联 | 正式报告 |
|---|---|---|---|---|---|
| HR | 2/3分段计算；block1被拒绝 | 成功段信号硬门通过，但单场次整体为`completed_with_failures`，且方法为BP工程冒烟 | `not_available`；未发现ECG | `blocked_pending_formal_release` | `blocked` |
| RR/BR | 2/3分段有`breath_rate` | `computed_pending_engineering_gate`；失败段拒绝 | `not_available`；未发现RSP | 阻断 | 阻断 |
| IBI/HRV | 2/3分段有候选 | `candidate_only`，不构成HRV质量通过 | `not_available`；无ECG beat/IBI参考 | 阻断 | 阻断 |

这些结果只能说明PR20输出契约和失败隔离行为在一个真实场次上可追溯，不能形成HR、RR、IBI或HRV生理结论。

## 8. 扩大门判断

**当前不通过扩大门。** 需要在扩大至39场/33组前完成：

1. 保持VMD缺失依赖为阻断，安装并锁定`vmdpy==0.2`后重新进行VMD真实单场次复跑；不得静默使用`bp_heart`替代正式方法。
2. 解释并修复或由负责人批准block1的`NO_VALID_CHANNEL_BIN_SELECTION`处理，不得把拒绝段伪装成成功段。
3. 重新确认单场次三段的JSON、segment表、质量同源性、状态字段和哈希均通过。
4. 保持`global`身份未冻结的边界；未来+72场必须重新生成全量匿名映射，不得拼接沿用当前33组身份。
5. 只有单场次重跑整体无失败且科学/数据负责人批准后，才可讨论增量生产；本任务不执行39场正式生产和任何正式统计。
