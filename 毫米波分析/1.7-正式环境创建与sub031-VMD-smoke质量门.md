# 正式环境创建与sub-031 VMD smoke质量门

> 日期：2026-08-29  
> 任务性质：正式阶段环境准备和单场次质量门验证。  
> 当前结论：VMD环境可用，但`sub-031`整体smoke质量门不通过；未启动39场全量生产。

## 1. 环境与代码基线

PR20隔离worktree为：

```text
D:\AAAWORK\07-竞赛\厚璨杯\验证工作树\毫米波-PR20-fc682b4
HEAD=fc682b491fcbeeb9bd1b030c8af9da33282d2846
状态：detached、干净
```

任务专用Conda环境原先不存在，本轮新建且未覆盖既有环境：

```text
D:\CondaEnvs\focuswave-mmwave-formal
```

实际安装包为`numpy`、`pandas`、`scipy`、`matplotlib`、`jsonschema`、`pytest`和锁定的`vmdpy==0.2`。环境内实测：`sys.executable=D:\CondaEnvs\focuswave-mmwave-formal\python.exe`，Python 3.13.15，`vmdpy=0.2`，`VMD`符号可调用；另有numpy 2.5.2、scipy 1.18.1、pandas 3.0.5、matplotlib 3.11.1、jsonschema 4.26.0、pytest 9.1.1。

环境记录位于：

```text
D:\_AttentionData\mmwave_local_execution\20260829_formal39_vmd_fc682b4\environment.yml
D:\_AttentionData\mmwave_local_execution\20260829_formal39_vmd_fc682b4\conda-explicit-spec.txt
D:\_AttentionData\mmwave_local_execution\20260829_formal39_vmd_fc682b4\environment-runtime.txt
```

对应SHA-256为：

| 文件 | SHA-256 |
|---|---|
| `environment.yml` | `88125F9E0EEB87EDF6064F85286EE691B87C6979B415DBFAD251D6C0EB46FA30` |
| `conda-explicit-spec.txt` | `38274A3A270F021DA51724BF49FEDAF247B320734AFEE22BB4CD61ACE9182183` |
| `environment-runtime.txt` | `42828DEED35E09235F9744BA2D3BC62FDE0640C4F917BF903FB954ACE5540E54` |

## 2. 环境创建过程、失败和修正

环境创建命令：

```powershell
conda create --prefix D:\CondaEnvs\focuswave-mmwave-formal python=3.13 pip -y
```

结果：退出码0，环境创建成功。

依赖安装命令：

```powershell
conda run --prefix D:\CondaEnvs\focuswave-mmwave-formal python -m pip install numpy pandas scipy matplotlib jsonschema pytest vmdpy==0.2
```

结果：退出码0，`vmdpy==0.2`和运行依赖安装成功。

第一次导出环境记录时误用了PowerShell不支持的`New-Item -LiteralPath`，导致记录目录未创建，随后导出命令报路径不存在；精确错误为`A parameter cannot be found that matches parameter name 'LiteralPath'`以及Conda导出路径`FileNotFoundError`。该次失败没有覆盖或删除任何文件。随后改用`New-Item -Path`创建新记录根并成功导出三份环境记录。

Smoke完成后的第一次多行JSON门检查误经`conda run ... python -c`传入，Conda返回`NotImplementedError: Support for scripts where arguments contain newlines not implemented`；没有改动产物。随后改用环境解释器绝对路径`D:\CondaEnvs\focuswave-mmwave-formal\python.exe`直接执行同一只读检查并成功完成schema、严格JSON和状态核验。

## 3. sub-031 VMD smoke

使用阶段0.2修正版manifest：

```text
D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_stage0_2_manifest.csv
SHA-256: 8408C14005885AF8E6955DF43A5B2887F8C8CD3BD26362F8DD0BDDAF1B8C9B63
```

smoke输出使用新隔离目录：

```text
D:\_AttentionData\mmwave_local_execution\20260829_formal39_vmd_fc682b4\smoke_sub031
```

实际命令为：

```powershell
conda run --prefix D:\CondaEnvs\focuswave-mmwave-formal --no-capture-output python -u scripts/run_timeline_gated_mmwave_quality.py --roots E:\正式实验 --input-manifest D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_stage0_2_manifest.csv --output-dir D:\_AttentionData\mmwave_local_execution\20260829_formal39_vmd_fc682b4\smoke_sub031 --run-analysis --method vmd_heart --analysis-id mmwave_formal39_sub031_vmd_smoke_v1
```

进程退出码为0，但程序结果为`n_sessions=1`、`n_segments_succeeded=2`、`n_segments_failed=1`，因此不能将退出码0解释为整体质量通过。

## 4. Smoke门检查

| 分段 | `algorithm_returned` | `quality_valid` | `selection_status` | `failure_reason` | 工程状态 |
|---|---:|---:|---|---|---|
| `baseline` | `true` | `true` | `selected` | `null` | HR=`signal_gate_passed`；BR=`computed_pending_engineering_gate`；IBI/HRV=`candidate_only` |
| `block_1` | `false` | `false` | `rejected` | `NO_VALID_CHANNEL_BIN_SELECTION` | HR/BR/IBI/HRV均拒绝 |
| `block_2` | `true` | `true` | `selected` | `null` | HR=`signal_gate_passed`；BR=`computed_pending_engineering_gate`；IBI/HRV=`candidate_only` |

输出契约核验结果：

- 5个JSON文件全部严格解析通过，原始字节扫描未发现`NaN`、`Infinity`或`-Infinity`。
- `segment_analysis_summary.json`通过`schemas/mmwave/segment_analysis_summary_v1.schema.json`验证。
- `segment_analysis_rows.csv`为3行、34列，保留三个分段和`algorithm_returned`、`quality_valid`、`selection_status`、`failure_reason`及分层状态字段。
- `anonymous_participant_group_id=apg-r-B4266D433438E0E4`从阶段0.2 manifest透传至manifest、summary和分段表；没有创建新的global身份。
- `input_manifest.sha256`在summary中为`8408c14005885af8e6955df43a5b2887f8c8cd3bd26362f8dd0bddaf1b8c9b63`，与修正版manifest一致。
- baseline和block2有VMD算法JSON；block1没有成功算法JSON，仅保留汇总拒绝记录，不把失败段填成空成功值。
- 输出目录共25个文件、约16.72 MB；绘图阶段产生既有Matplotlib `tight_layout`警告，已保留在运行过程记录中，未静默为成功。

Smoke关键输出哈希为：

| 文件 | 大小 | SHA-256 |
|---|---:|---|
| `crop_manifest.json` | 2,477 B | `9FC0CE02FCED81F0BB520D29464EE6107C08234B9992946C5D38FB4FE3721B52` |
| `crop_summary.json` | 876 B | `2683CE3B3B0AF09DECD9B5F9707A880D9A1C87C304F76EC97DBAAC031458A8A8` |
| `segment_analysis_summary.json` | 1,468,110 B | `FB55FFFCF3F2775914CFDED1CC2B89E1C4C41CBF97FF8AB018F09DBF0E9629D0` |
| `segment_analysis_rows.csv` | 1,575 B | `5E749A3A8DA32ABF8D35AE2EE49663FA6EA961CDF8714946F38CDCAF1D491633` |

## 5. 指标边界

本次VMD smoke只证明专用环境、输入读取、分段、VMD算法调用、状态落盘和JSON/schema契约可以运行，不构成生理效度结论。

- HR：baseline和block2可计算且当前信号工程门通过；block1拒绝；外部ECG验证为`not_available`，不可进入正式报告。
- RR/BR：baseline和block2返回候选，但工程门仍为`computed_pending_engineering_gate`；没有RSP外部验证。
- IBI/HRV：成功段仅为`candidate_only`；无参考级ECG/IBI验证，不进核心结论，不作生理解释。
- 行为关联和正式报告状态继续阻断。

## 6. 正式39场运行决策

**不启动39场全量生产。** 原因不是VMD环境不可用，而是smoke整体质量门未通过：`block_1`仍有`NO_VALID_CHANNEL_BIN_SELECTION`。在代码/Git任务解释该失败段或完成批准的修复、重新单场验证整体无未解释失败前，不能进入39场运行。

同时保持以下约束：5个无效/空输入必须进入结构化缺失表；39/44和33个当前队列匿名组的分母不变；每个baseline/block独立切分；HR和BR为主分析；IBI/HRV不作外部验证结论；BP与VMD轨道分开；RGB运动仅作为未来/敏感性轨道；所有正式输出写入新的空目录，不覆盖历史。

本轮未修改代码、配置或源报告，没有pull、merge、切分支、commit或push。PR20 worktree保持detached且干净。
