# 09-25-2 行为 20 秒与 30 秒窗口同样本配对验证

日期：2026-09-25

状态：**PASS（真实运行与独立核验完成）**
方法：[`1.16.29-行为20秒与30秒同样本配对验证_20260925.md`](../分析设计/1.16.29-行为20秒与30秒同样本配对验证_20260925.md)

来源边界：本记录及方法最初以 Formal 交接提交 `7296d751ca40fcfd4ccb4874c432bbfbaf4d65c0` 留在 `codex/code-fix-ledger`；该分支与当前权威 `main` 已分叉。本次只对方法、运行记录和必要索引做内容级迁移，未合并旧分支的其他修订。以下运行数字与原结果文件不改写。

## 输入与执行

本轮只比较行为参照模型的 20 秒和 30 秒窗口。原 30 秒主分析保持冻结；本轮在共同的 **61 名参与者、116 场、2,306 个探针**上分别完整重训两个窗口。原 30 秒分析集合有 2,316 个有效探针，其中 10 个不在本次共同集合；不能复用原 2,316 探针模型的折外预测来计算本次差值。

| 来源 | SHA-256 |
|---|---|
| `SupervisedRunsWindowSensitivityV1/inputs/AS.behavior_reference__window20s.csv` | `76c653f85f741e801dd5e118ed685ac6678825a046d32f7e196ba3b3f070ebdb` |
| `SupervisedRunsV1/inputs/AS.behavior_reference.csv` | `6de2c35edd4741d242e0ed23a5eca1bca693e2d932e4dc673f9a32db91aa192b` |
| `Behavior/formal_v3/probe_window_sensitivity.csv` | `e9594dd616b5583dde9e3e167e6879b951625d9777c07f3b09aaa31a5d33a68d` |

代码仓库：`kyandi233-dev/Attention-Analysis`；本机工作树 `codex/predefined-reduced-nested-loso-20260925`，运行时基线提交 `18e6678c4569ce96a1d33fbd4458a8a4743d984b`。新增入口在运行后以提交 `d31cedfe673270f39f0438e4958ead7f4f228a5a` 推送到远端既有 `codex/formal-analysis-v2-portable` 分支；运行脚本现场 SHA-256 为 `afe806cd11f02432f04c4f5be119603490365ccf31560c8299d2693f04133677`。配置 `configs/supervised_learning_v1.yaml` 的 SHA-256 为 `2542e24d931db4bd7d53e04188ca43faafbad295985c43740f8258377e4d17b2`。入口为 `scripts/behavior_window_paired_2030.py`，独立核验为 `scripts/verify_behavior_window_paired_2030.py`。随机种子 `20260910`；参与者分组内层五折；正则候选 `C = {0.01, 0.1, 1, 10}`；固定折外参与者簇自助重采样 1,000 次，种子 `20260830`。

执行解释器：`C:\Users\550ACW\anaconda3\python.exe`。两个运行清单分别记录完成时间 `2026-09-25T08:56:38Z` 与 `2026-09-25T08:57:38Z`；执行命令从代码工作树根目录运行，`PYTHONPATH=src`，依次传入上述 20 秒集合、冻结 30 秒集合、原始窗口敏感性表、`configs/supervised_learning_v1.yaml` 和下述结果根目录，再使用独立核验入口对该结果根执行 `--run-root`。

本地结果根：`D:\Project\厚粲杯\11_数据\_FormalAnalysis\SupervisedRunsWindowPaired2030V1\`。包含两个窗口的共同样本输入、各自 `probe_predictions.csv`、`fold_audits.json`、`run_manifest.json`，以及根目录 `paired_participant_delta.csv`、`paired_summary.json`、`paired_verification.json`。共 31 个文件、17,686,258 字节；逐探针与参与者明细为 local-only，不进入 Git。

## 结果

| 共同样本上的窗口 | 参与者等权平均对数损失 |
|---|---:|
| 20 秒 | 0.631840 |
| 30 秒 | 0.624089 |

差值定义为 **30 秒损失 − 20 秒损失**，估计值为 **−0.007751**；95% 置信区间（confidence interval [CI]）为 **[−0.015151, 0.002301]**。点估计偏向 30 秒，但区间跨 0；当前没有明确证据表明两种窗口在共同样本上的跨参与者概率预测损失不同。该结果不构成统计等效检验，也不证明 30 秒最优。30 秒继续作为事先冻结的主窗口。

## 验收与边界

- 两臂输入均为 2,306 个唯一且完全相同的探针键；Q1 标签与 Q2 上下文一致。两臂五项特征各自与原始窗口切片逐值一致，容差 `1e-9`。
- 每臂 61 个外层参与者折、2,306 条有效折外预测，失败折为 0；外层训练与测试、内层训练与验证均参与者互斥，两个窗口的内层划分一致。
- 独立核验从预测概率重算参与者损失、成对差值及 1,000 次重采样区间，结果与汇总文件在 `1e-12` 容差内一致，`paired_verification.json` 状态为 `PASS`。
- 新 20 秒臂与 `1.16.25` 既有 20 秒臂在同 2,306 个探针上的折外概率最大绝对差为 `4.996e-16`，验证当前重训入口复用了既有训练与划分口径；30 秒臂则在共同样本上重新训练。
- `paired_summary.json` SHA-256：`C205A5BE8A1200C69823F6F5F29907104A129E489A6779509587C483B64E600F`；`paired_verification.json`：`14E4FC802DF68C09E6635C2E429EA004393FC90AF78D1F346ACA4CC8F166EE17`。
- 本分析只覆盖行为端 20 秒与 30 秒；不评价 10 秒、眼部、动作或完整多模态窗口效应。固定折外重采样反映当前预测档案的参与者间不确定性，不包含重训模型产生的额外不确定性。
