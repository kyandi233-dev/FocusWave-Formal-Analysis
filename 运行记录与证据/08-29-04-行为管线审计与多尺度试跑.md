# 行为管线审计与多尺度试跑

> 2026-08-29（Asia/Shanghai）｜本地数据执行任务
> 总结：完成现有行为资产、BB代码、旧BBB输出、原始试次字段和重复注册表的只读核验；用`sub-031`/`sub-032`完成不依赖最终身份表的BB工程试跑；未运行44场次统计，未修改代码仓库、原始数据或源报告。

## 一、原计划

背景是正式分析需要把行为层从既有探针前窗口工程特征扩展为可审计的多尺度候选体系，同时必须先区分历史BBB结果、当前BB原始数据和中央canonical行为锚点。目标是核验实现是否覆盖探针窗口、周期/区块和场次尺度，明确Go/No-Go信号检测指标的口径与分母，检查错误/遗漏/RT/错误前后轨迹/Q1/Q2和重复参与者接口，并设计一个不依赖最终身份冻结的最小试跑。

原定步骤为：先读入口、分析设计和协作治理；核验仓库状态和权威路径；只读检查代码、字段、历史结果和重复注册表；在1–2个匿名场次级标识上做结构和计算契约试跑；把可复用内容、缺口、放行条件和等待条件写入行为分析文档；保留工作树变更并交给专门Git任务处理。

风险和停止点是：不得把场次当自然人；不得把旧BBB或12项探针特征写成当前完整体系；不得在身份冻结前做44场次正式推断；发现需要pull、切分支、覆盖结果、修改科学定义、身份冲突或字段变化时停止。

## 二、执行与决策过程

### 1. 入口和治理核验

已读取：

- `D:\aaawork\07-竞赛\厚璨杯\AGENTS.md`；
- `D:\aaawork\07-竞赛\厚璨杯\000-项目定位和进度\000-厚璨杯项目记忆.md`；
- `D:\aaawork\07-竞赛\厚璨杯\040-竞赛报告与多模态分析\正式分析完整管线与报告证据库\README.md`、资产导航、分析设计、行为分析README；
- `D:\aaawork\07-竞赛\厚璨杯\040-竞赛报告与多模态分析\正式分析完整管线与报告证据库\运行记录与证据\1.2-协作角色、任务分工与Git治理.md`。

新的协作治理把本任务限定为本地数据执行：不pull、commit、push、切分支，不自行改变科学定义，不进行正式44场次统计。该边界在本次执行中得到遵守。

### 2. 仓库和输出资产核验

`focuswave-central`当前为`codex/mmwave-formal-reanalysis-v2`、HEAD `d87229afe071f23450728a6d617ec82317e6c9df`、工作树干净；其现有行为baseline为70 session/46 natural participants/1,400 probes的北京canonical资产，使用30秒主窗口和参与者分组折，不能直接替代本地44场次原始行为重建。

`Attention-Analysis-0816-history`当前为`nvidia-cuda`、HEAD `14d33a76a53806c5d5366cd46e06b7f8660a121b`、工作树干净；其`behavior_formal`代码是可执行的当前BB历史参考，但默认路径为`J:/Data`和`D:/Project`，本次只读试跑使用内存配置显式指定`E:\正式实验`，没有修改配置。

`D:\_AttentionData\output-v2\050-sart-formal`现场存在完整旧输出，但`051-block_metrics.csv`为60行且含3个Block；旧报告正文使用其自身主队列/排除项口径，不能从这60行直接推断参与者数；`051-cycle_bin_metrics.csv`为360行，`051-probe_behaviour_link.csv`为600行。该资产与当前B1/B2、编号≥31的正式边界不一致，决策为仅保留历史provenance，不搬用数值。

`ad11f1d`属于正式分析完整管线与报告证据库自身的治理基线，已按该证据库当前治理记录核验。`focuswave-central`、`Attention-Analysis-0816-history`等代码仓库分别记录自身分支/HEAD，代码与证据库通过运行记录、路径和运行ID关联。本次未执行pull、fetch、commit、push或切分支。主项目根仓库本来就存在大量与本任务无关的用户修改/未跟踪文件，未触碰、未清理、未stash、未reset。

### 3. 原始数据和重复注册表核验

`E:\正式实验`共有65个编号目录、151个行为文件。按编号≥31并排除`sub-9504`后，当前原始行为候选为44个采集场次（44个场次级标识；唯一自然参与者数待重复身份冻结）；每个场次级标识B1/B2各432行、48个No-Go、10个探针。`sub-9504`有B1/B2/B3三Block，作为试运行场次必须显式排除，不能由默认目录发现逻辑自动纳入。

原始行为CSV字段数为27，字段结构在四个试跑文件中一致，覆盖试次键、周期、刺激大小、Go/No-Go、RT、错误/遗漏、探针Q1/Q2和真实时间戳。行为原始根保持只读。

`D:\_AttentionData\repeat_participant_registry_v2`及corrected副本均为32个临时重复组、90个session；`identity_status`为临时确认，`global_repeat_participant_id`为空。当前44个候选采集场次只有31个能按`subject_id`找到注册表行，13个缺失，因此没有使用注册表生成折或正式分组推断。

### 4. 代码和计算契约审计

BB代码实际覆盖：Block层commission/omission、d′/c/β、RT中位/均值/SD/CV、ex-Gaussian；周期bin层错误率、Jeffreys区间、RT中位和计数门控CV；探针层此前最多8个正确Go的RT中位/均值/CV和Q1/Q2原始码。代码没有场次尺度统一RT表、各尺度RT斜率、周期/探针层d′/c/β，也没有探针秒数窗口的完整分母表。

Go/No-Go契约核验为：命中=正确Go，虚警=No-Go commission；Go机会数和No-Go机会数分别由`is_no_go==0/1`切分；比例使用`(successes+0.5)/(opportunities+1)`；d′、c、β分别使用正态分位数差、反应标准和正态密度比。极端比例输入试算不产生无穷值，但现有实现没有较低机会数的统一估计门。

当前BB配对统计和MixedLM仍以`subject`为键或分组键，未接入`repeat_participant_id`；因此只能作为身份冻结前的工程结构试跑，不能直接形成重复场次推断。

### 5. 最小安全试跑

实际运行命令逻辑为：以`D:\Code\python\python.exe`调用历史仓库`src`，内存构造BB配置，将数据根指向`E:\正式实验`，只加载`sub-031`、`sub-032`的B1/B2；执行`load_cohort`、`validate_formal`、`formal_block_metrics`、`cycle_bin_metrics`和`probe_behaviour_link`，输出到一个此前不存在的新目录，目录已存在时拒绝覆盖。

试跑manifest：`D:\_AttentionData\output-v2\050-sart-formal-audit\20260829_behavior_smoke_sub031_sub032\run_manifest.json`。

结果为：1,728试次、4行结构验证、4行Block指标、24行周期bin指标、40行探针链接。所有断言通过；默认发现逻辑的只读检查得到45个场次级标识且包含`sub-9504`，形成正式运行前的样本门控证据。输出文件SHA256已写入manifest，试跑明确标注`engineering_smoke_only`、`no_repeat_identity_used`和`no_formal_inference`。

### 6. 测试结果

在`Attention-Analysis-0816-history`中执行：

```text
$env:PYTHONPATH='src'; D:\Code\python\python.exe -m pytest -q tests/test_behavior_formal_bb.py
```

结果为3 passed。该测试覆盖B1/B2配对而非B3、缺失根发现和重复行为根拒绝。

同时执行了历史行为全套若干测试，结果出现6个失败，失败原因均为预实验测试夹具试图读取不存在的`sub-000`的A Block文件（`FileNotFoundError`），不是本次BB两个场次级标识试跑的失败。该结果记录为环境/测试数据缺失，不通过复制数据或改写测试来掩盖。

## 三、最终决策结果

1. 既有12项探针窗口工程特征不作为完整行为指标体系；完整体系至少需要试次、探针秒数窗口、周期/区块和场次四层，并明确RT水平、离散、CV、斜率、错误成分、d′/c/β和Q1/Q2的分母与状态。
2. 旧`050-sart-formal`输出标为历史BBB不匹配资产；其图、p值和3Block结论不进入当前正式行为分析。
3. BB提取、结构验证、Block指标和部分周期/探针函数标为“可复用工程骨架”，不是已冻结正式生产器。
4. 本次两个场次级标识结果标为“仅工程试跑”；它证明接口可以在本机真实文件上运行，不证明指标效度、重复测量稳定性或正式样本结论。
5. 在最终重复身份和44场次样本manifest冻结前，停止所有全队列行为统计、重复场次模型和参与者互斥折评估。

## 四、已完成、未完成和待确认

### 已完成

- 入口、资产导航、协作治理、仓库状态、历史输出和原始字段只读核验；
- 多尺度实现矩阵、Go/No-Go及d′/c/β计算契约审计；
- 重复注册表覆盖和当前44个采集场次/场次级标识交集审计；
- `sub-031`/`sub-032`最小BB工程试跑及manifest、CSV输出；
- BB专门单元测试3项通过；
- 新增行为分析审计文档和多尺度冻结条件文档。

### 未完成

- 未生成最终44场次标准化表；
- 未计算正式44场次d′、c、β、RT-CV、RT斜率或任何p值/AUC；
- 未建立最终`repeat_participant_id`、共同样本和参与者互斥折；
- 未修改历史代码以补齐场次RT、秒数探针窗口、RT斜率或低分母门；
- 未写入或修改正式报告。

### 必须等待的门控条件

- 样本冻结任务交付最终44场次manifest、`sub-9504`排除规则和session键；
- 中央身份任务交付最终重复参与者映射、身份状态和折分规则；
- 科学/监督任务冻结探针窗口、RT多尺度定义、斜率算法、机会数门和Q1/Q2口径；
- 专门Git任务确认可提交文件、关联代码提交和运行证据后，才处理本工作树的文档提交。

## 五、当前工作树精确变更清单

本地数据执行任务新增、尚未提交的文件仅为：

1. `D:\aaawork\07-竞赛\厚璨杯\040-竞赛报告与多模态分析\正式分析完整管线与报告证据库\行为分析\1-现有管线与指标资产审计.md`
2. `D:\aaawork\07-竞赛\厚璨杯\040-竞赛报告与多模态分析\正式分析完整管线与报告证据库\行为分析\1.1-多尺度行为指标试跑与冻结条件.md`
3. `D:\aaawork\07-竞赛\厚璨杯\040-竞赛报告与多模态分析\正式分析完整管线与报告证据库\运行记录与证据\08-29-04-行为管线审计与多尺度试跑.md`

没有代码仓库源码变更，没有配置变更，没有原始数据变更，没有旧结果删除/覆盖，也没有Git提交、推送、拉取或切分支。新增本地数据试跑目录是：`D:\_AttentionData\output-v2\050-sart-formal-audit\20260829_behavior_smoke_sub031_sub032`。
