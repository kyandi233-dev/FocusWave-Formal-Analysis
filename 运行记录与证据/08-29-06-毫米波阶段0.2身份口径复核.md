# 毫米波阶段0.2身份口径复核

## 总结

本次作为本地毫米波数据执行任务，对PR20 `sub-031`最小复跑的manifest、已有毫米波说明和后续合并/模型门控进行了只读语义复核。确认当前队列主口径为44个session、38个当前队列匿名参与者分析组、6个双场重复组；毫米波仍为39/44个场次可加载、联结后对应33个当前队列分析组；036/038/040/041为32字节`.bin`和0字节timestamps无效占位，047目录为空。没有运行新算法、没有运行39场正式生产、没有进行正式统计或形成HR/RR/IBI/HRV生理结论。

E2姓名作为主匹配并以手机号/学号交叉核验，Q3匿名键独立复核；旧R2 corrected registry只覆盖31/44，仅作敏感性辅助。13个R2外场次是当前队列单场分析组，不是身份未知。现有`anonymous_participant_group_id`保持不变，`global`跨队列身份仍未冻结；未来约72场次接入时须全量重建映射。历史manifest和历史输出未被覆盖。

## 原计划

1. 核对旧manifest、PR20输出和已有毫米波1、1.1、1.2、1.4说明中的身份字段与样本口径。
2. 确认身份语义纠正不改变匿名组号、session键、算法数值、质量层级和扩大门。
3. 在不运行算法的前提下，生成一个阶段0.2语义修正版单场次manifest，并保留旧manifest和输出哈希。
4. 更新毫米波说明及本任务工作记录，明确后续模型门控和停止条件。

## 执行与决策过程

### 1. 旧产物核验

旧试跑manifest为：

```text
D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_frozen_manifest.csv
SHA-256: 875464A7D5D2610559AB88D2E988FC701EEC992269AFF32D4AD582C5B7224DA9
```

旧BP工程冒烟输出目录为`D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_bpheart_fc682b4`。复核到旧输出仍可严格解析，`crop_manifest.json`保留`session_id=sub-031`和匿名组键；具体匿名组键仅保留在数据区产物中。旧身份状态仍为`provisional_registry_history`，这是历史运行元数据，不回写。旧输出关键哈希复核为：`crop_manifest.json=1FB4AFF58265281C0C82370969DB6CCC3BBEA3A2828002407C1A8812551E984E`，`segment_analysis_summary.json=F0809B8AE3F194400A3E666178DE4B689C5BC2302EF09F837CD586A00D9F9FC5`。

### 2. 阶段0.2修正版manifest

实际执行的是只读旧manifest后导出新文件的PowerShell命令；没有调用毫米波生产脚本：

```powershell
$correctedManifest = 'D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_stage0_2_manifest.csv'
$sourceManifest = 'D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_frozen_manifest.csv'
$row = Import-Csv -LiteralPath $sourceManifest | Where-Object { $_.session_id -eq 'sub-031' }
$row.identity_status = 'frozen_current_cohort_anonymous_group'
$row.evidence_source_code = 'E2_primary+Q3_independent;R2_sensitivity_only'
$row | Export-Csv -LiteralPath $correctedManifest -NoTypeInformation -Encoding UTF8
Get-FileHash -LiteralPath $correctedManifest -Algorithm SHA256
```

结果为1行，`session_id=sub-031`，匿名组键保持不变且具体值只在数据区；新文件为：

```text
D:\_AttentionData\mmwave_local_execution\20260829_pr20_sub031_stage0_2_manifest.csv
SHA-256: 8408C14005885AF8E6955DF43A5B2887F8C8CD3BD26362F8DD0BDDAF1B8C9B63
identity_status: frozen_current_cohort_anonymous_group
evidence_source_code: E2_primary+Q3_independent;R2_sensitivity_only
confidence: medium（沿用旧值，未擅自升级）
```

`frozen_current_cohort_anonymous_group`只是本次阶段0.2当前队列语义标签，不构成中央受控枚举的擅自变更；后续以中央合并契约为准。没有使用该修正版manifest重跑，因此不存在与它对应的新算法输出或新数值结果。

### 3. 说明与门控修订

已更新正式证据库毫米波文档`1`、`1.1`、`1.2`、`1.4`，新增`1.5-阶段0.2身份口径对manifest与模型门控的纠正.md`。说明已明确：`subid`、`subject_id`、`experiment_id`是session键，不是自然参与者键；`presence`是模态输出存在性，不是签到或到场字段；当前38组不是永久全球身份；未来约72场次必须全量重建映射。

后续门控没有放宽：VMD因缺少`vmdpy==0.2`保持`blocked`；BP仅是工程冒烟；历史`sub-031`的block1仍为`NO_VALID_CHANNEL_BIN_SELECTION`拒绝；严格JSON、有限数值、同源质量、外部ECG/RSP验证、行为关联和正式报告资格仍按原分层执行。HR、RR/BR、IBI/HRV的“可计算、工程质量、外部生理验证、行为关联、正式报告”不能合并成一个真假字段，IBI/HRV不作生理结论。

## 最终决策结果

- 身份口径已按阶段0.2纠正为44/38/6和毫米波39/33/6；5个无效/空输入事实不变。
- 旧manifest、旧输出及其哈希保持不变；修正版manifest为新增、未运行派生件，匿名组号未改变。
- 没有修改中央代码、配置或源报告，没有安装依赖，没有pull、切分支、commit或push。
- PR20隔离工作树仍为detached且干净；正式证据库其他并行任务改动未清理、未覆盖。
- **不通过扩大门**：本次语义修正不等于新运行验收，不放行39场/33组正式生产或44场正式统计。待中央确认全量身份/合并契约、VMD正式方法和后续单场次重跑门后再决定扩大。

## 待确认事项

1. 中央治理任务需确认阶段0.2全量匿名映射的正式文件、受控`identity_status`枚举、`merge_key`和空值编码。
2. 如需元数据完全对齐的单场次输出，应使用修正版manifest在全新空目录重新运行并重新计算哈希；该动作当前未获扩大放行。
3. 未来约72场次接入前必须全量重建映射，不能直接沿用当前38个分析组或当前33个毫米波可用组。
