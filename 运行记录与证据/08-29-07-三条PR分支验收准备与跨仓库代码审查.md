# 三条 PR 分支验收准备与跨仓库代码审查

## 总结

本次作为专门 Git 监督任务，隔离获取并审查 NIR、正式 BB 行为和毫米波生产契约三个 pull request（PR）分支。已核验目标基线、代码差异、测试声明、CI 配置、关键门控和本地最小复跑落点。没有合并、推送、切换或污染用户现有工作树，也没有读取或修改原始数据区。

结论：三个 PR 均通过代码级验收，可进入本地最小复跑；三个 PR 均未通过正式科学统计或报告准入。毫米波 VMD 在当前环境因缺少 `vmdpy==0.2` 被正确阻断，未自动降级。

## 原计划

1. 读取项目入口、统一项目记忆和协作治理。
2. 在独立临时审查区获取 PR 分支和目标基线。
3. 核验 diff、提交、测试、CI 和关键科学门控。
4. 运行不读正式数据的定向测试与依赖预检。
5. 写入模块验收记录和本总记录，保留未提交状态。
6. 检查链接、编码、敏感信息、差异和工作树，不合并、不推送。

## 实际核验与结果

| 模块 | 头提交 | 目标基线 | 实测证据 | 结论 |
| --- | --- | --- | --- | --- |
| NIR | `f89ff57d4ddfa5c375174ee64148c10f8cc297e1` | `b2a96c0df6e6db88c599332815fb24cc93c07fe0` | 定向测试 12 passed；CI 配置含适配器测试 | 可最小复跑，正式统计阻断 |
| 行为 BB | `0423beba1813bf81b16d4d7d7e9c7ac4763920e6` | `155a1aa9f38f9b9b7103c290660a1769e11dda7e` | 定向测试 7 passed；fixture 为 4 场/3 组 | 可最小复跑，须核对 44 场 manifest |
| 毫米波 | `fc682b491fcbeeb9bd1b030c8af9da33282d2846` | `d87229afe071f23450728a6d617ec82317e6c9df` | 定向测试 11 passed；VMD 预检阻断，BP 预检通过 | 可最小复跑，VMD 依赖未满足 |

Git 远端复核结果：NIR 分支仍指向 `f89ff57...`，其目标分支 `analysis/multimodal-integration` 仍指向 `b2a96c0...`；行为分支仍指向 `0423beb...`；毫米波两个分支仍分别指向 `fc682b4...` 和 `d87229a...`。正式分析仓库 `main` 与 `origin/main` 均为 `171b081f3a3f9d06496c7b8d36915eebd4e2a3bb`。

PR 网页/API 在本次环境返回 404，因此只记录 Git transport 和本地测试证据，不宣称远端 CI 已通过。

## 风险、停止点与未决事项

- NIR 适配器测试不等价于真实 27 场或 44 场运行；不得把 pupil-only 适配器通过写成 PIR/OAR 或生理效度结论。
- 行为必须实际核对 44 场、38 个匿名分析组和 6 个双场重复组；旧 BBB 不得冒充当前 BB。
- 毫米波继续使用 39 场/33 组可加载口径；`sub-036`、`sub-038`、`sub-040`、`sub-041` 的 32 字节 bin 加 0 字节 timestamp 和 `sub-047` 空目录必须排除。
- 本地负责人需在全新输出根完成最小真实复跑并回传产物、哈希和失败明细；监督确认前不得合并 PR 或推送。
- 若出现远端新提交、用户工作树变化、PII、科学矛盾或范围外文件，立即停止，不自行合并、删除或整理历史。

## 文件与状态

本记录及三个模块验收记录均以未提交状态保留，供监督检查。既有历史记录未删除、未重命名、未改写。

