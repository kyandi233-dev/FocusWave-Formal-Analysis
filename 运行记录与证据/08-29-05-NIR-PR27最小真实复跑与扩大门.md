# NIR PR27最小真实复跑与扩大门

日期：2026-08-29

角色：本地NIR数据执行任务

## 一、总结

在隔离工作树D:\AAAWORK\07-竞赛\厚璨杯\验证工作树\NIR-PR27-f89ff57、HEAD=f89ff57d4ddfa5c375174ee64148c10f8cc297e1下，使用D:\Code\python\python.exe完成PR27瞳孔-only适配器的定向测试和四个场次级标识的最小真实复跑：sub-031 schema v7、sub-032/sub-033特殊来源 schema v6、sub-035历史YOLO b8/RITnet b16 schema v6。

定向测试为12个通过。最终隔离复跑合计输出328,133行；四个场次级标识的源hash均核对一致，主键重复0，unix_ms单调性失败0，输出无PIR/OAR-like列，显式PIR/OAR拒绝抛出IrisGeometryUnavailableError。current/previous视觉属性、行为时间连接、失败原因和质量轨道均已保存。

结论不是“44场次可运行”。本次只通过了小样本工程复跑，不通过正式44场次扩大门。

## 二、原计划

1. 读取PR27隔离工作树入口、正式证据库NIR分析1–1.3和08-29-07记录。
2. 验证D:\Code\python\python.exe及真实输入路径。
3. 只读复跑031、032、033和实际历史YOLO b8场次级标识，使用新建空时间戳输出根。
4. 核验schema、source/path/eye_raw、主键、时间单调、current/previous视觉属性、匹配距离/失败原因、QC分轨和PIR/OAR拒绝。
5. 保存命令、配置、输入manifest、输出manifest、hash、行数、失败明细和少量测量证据图。
6. 不运行27/44场正式统计，结果保留未提交并回传扩大门判定。

## 三、执行与决策过程

### 3.1 入口与输入

已读取PR27工作树AGENTS.md、README.md、runtime/nir-formal/README.md、docs/020-nir/README.md，以及正式证据库NIR分析/1、1.1、1.2、1.3和运行记录08-29-07。工作树核验为detached且干净。

PR27模板默认行为路径D:\_AttentionData\Beijing-Behavior\formal-v1\trial_level.csv不存在。实际使用D:\_AttentionData\Beijing-NIR\analysis\nir-rgb-fusion-validation-v2\analysis_tables\subjects\sub-XXX\sub-XXX_trial_level.csv；每个场次级标识864行。原始行为表只读，字段最小化副本和映射记录保存在：

D:\_AttentionData\Beijing-NIR\validation\pr27-real-rerun-20260829T125527-phasefix-input

### 3.2 两轮隔离复跑

第一输出根D:\_AttentionData\Beijing-NIR\validation\pr27-real-rerun-20260829T125014保留了启动/字段连接问题：未设置PYTHONPATH时为ModuleNotFoundError；随后显式设置PYTHONPATH和UTF-8后适配器输出完成，但因phase_segment未接入行为表而全部为no_behavior_group。

第二输出根D:\_AttentionData\Beijing-NIR\validation\pr27-real-rerun-20260829T125527-phasefix第一次尝试触发phase_segment int/float merge_asof类型错误。最终将phase_segment设为int64 1，并将行为时间键设为float64后，四个场次级标识成功输出。

该过程证明：真实数据字段语义和dtype均属于扩大门必审契约，不能由“脚本退出成功”替代。

### 3.3 最终结果

| 场次级标识 | schema | 行数 | behavior matched | no_behavior_group | failed | current visual matched | previous visual matched |
|---|---:|---:|---:|---:|---:|---:|---:|
| sub-031 | 7 | 81,830 | 67,143 | 14,667 | 20 | 67,143 | 66,986 |
| sub-032 | 6 | 80,553 | 66,158 | 14,377 | 18 | 66,158 | 66,006 |
| sub-033 | 6 | 83,822 | 69,309 | 14,493 | 20 | 69,309 | 69,153 |
| sub-035 | 6 | 81,928 | 67,050 | 14,860 | 18 | 67,050 | 66,893 |

no_behavior_group来自无行为trial的baseline/instructions/practice；failed主要为block首trial前记录，均已写入failure_details.csv。没有把这些记录改写为成功或删除。

### 3.4 代码级问题记录

PR27脚本输出manifest的formal_evidence_commit硬编码为171b081f3a3f9d06496c7b8d36915eebd4e2a3bb，没有忠实记录本次PR27工作树f89ff57d4ddfa5c375174ee64148c10f8cc297e1。另发现适配器没有内部统一phase_segment和时间merge key dtype。两项均未在本地修改，已记录为最小代码修复建议和回归验证条件。

## 四、最终决策

1. 通过四个场次级标识的隔离式结构复跑，证明PR27可以读取实际schema v6/v7来源并生成瞳孔-only输出。
2. 通过source hash、输出hash、主键、时间单调、eye_raw、current/previous视觉属性、匹配距离/失败原因、QC分轨和PIR/OAR拒绝验收。
3. 不通过正式44场次扩大门，原因是提交provenance硬编码、dtype契约未内置、正式行为源路径未冻结、最终身份表未冻结。
4. 不运行27/44场正式统计，不报告自然参与者数量，不写生理或心理结论。

## 五、交接产物

最终输出根：

D:\_AttentionData\Beijing-NIR\validation\pr27-real-rerun-20260829T125527-phasefix

其中包括：

- 每个场次级标识的pupil_only_rows.csv和manifest.json；
- postrun_validation_summary.json；
- failure_details.csv；
- pir_oar_refusal.json；
- sub-031、sub-032、sub-033、sub-035各一张measurement_evidence.png。

输入与命令日志根：

D:\_AttentionData\Beijing-NIR\validation\pr27-real-rerun-20260829T125527-phasefix-input

其中保存四份场次级标识source manifest、四份配置、四份字段最小化行为输入、input_derivation_manifest.json及运行日志。

正式证据库新增结果文档：

NIR分析/1.4-PR27最小真实复跑结果与扩大门判定.md

本记录和新增结果文档均保留未提交，未执行Git管理操作。后续需先完成元数据/dtype/权威行为源修复和回归测试，再由正式角色重新判断扩大门。

## 阶段0.2身份口径追加说明（2026-08-29）

本记录前文形成时将重复身份冻结作为未完成门控；阶段0.2现已纠正当前队列主口径。当前正式边界为44个session、38个当前队列匿名参与者分析组，其中6个双场重复组涉及12个session。13个未进入旧R2 corrected registry的场次属于当前队列单场分析组，不是完全未知；旧R2仅覆盖31/44，只作敏感性辅助。该追加说明不改写本次复跑的四个场次级标识结果，也不把四场复跑扩大为44场正式统计。

NIR四份输出manifest是场次级技术provenance，未写入参与者分组字段，因此没有改写manifest内的组号。现依据数据区既有mapping manifest和CSV生成四场只读联结审计：`identity_join_audit.json`。后续模型/交叉验证必须使用现有`anonymous_participant_group_id`按38组分组；`subid`、`subject_id`、`experiment_id`均为session键，`presence`只表示模态输出存在性，不是签到。未来约72场接入时必须全量重建映射，当前38组不是永久全球身份。

该身份口径修订不改变此前已记录的技术阻塞：`formal_evidence_commit`仍被PR27硬编码为基线提交，merge key dtype仍未在适配器内部统一，正式权威行为源仍未冻结。因此扩大门仍暂不通过；只需移除“身份冻结尚未完成”这一旧阻塞表述，不能据此提前放行44场正式统计。

## 44场只读全量预检追加（2026-08-29）

在用户放行正式44场分析后，本地数据执行任务先完成只读全量预检，不启动正式统计。输入路由固定为：`sub-031`使用`validation/pupil-geometry`，`sub-032`和`sub-033`使用`amd-directml/ritnet-fullclass-final`，其余41个session使用`historical-yolo/ritnet-fullclass-final`。

全量结果为44/44完成，eye metrics共3,538,056行，frame coverage共1,805,423行；schema为eye v6共43场、v7共1场（031），frame v2共44场。completion、manifest、summary、eye metrics、frame coverage、QC索引和像素证据均存在，逐场声明hash与实际hash一致，completion/summary行数一致；eye/frame主键重复均为0，`unix_ms`单调失败组均为0。预检结构性错误为0。

当前正式身份口径沿用数据区现有`anonymous_participant_group_id`：44个session、38个当前队列匿名分析组、6个双场重复组涉及12个session。13个R2外场次是当前队列单场分析组，不是完全未知；旧R2仅覆盖31/44，只作敏感性辅助。`subid`、`subject_id`、`experiment_id`是session键；`presence`是模态输出存在性，不是签到；未来约72场接入时须全量重建映射。

共同provenance核验到RITnet输入640×400、fp32、batch size 16、模型SHA256、ROI算法和replicate padding均有记录且当前值一致。031仅按`validation/pupil-geometry`来源标记topology主几何；基础版、旧版和EllSeg版仍只做紧凑方法对比。4个警告仅为sub-031至sub-034的来源YOLO batch/model hash未记录，不是核心输出缺失或hash不一致。

新的隔离输出根为：

`D:\_AttentionData\Beijing-NIR\validation\formal44-preflight-20260829T142635`

其中保存`nir_formal44_input_manifest.json`、CSV、Parquet、schema、source schema inventory、QC汇总、两张描述性图、运行日志、预检脚本和`SHA256SUMS.txt`。Parquet最初因pandas环境缺少pyarrow/fastparquet失败，随后使用既有Polars引擎成功写入44行×46列；该过程和最终hash均保留在日志/哈希清单中。

本次全量预检通过来源/QC结构门，但正式统计仍受代码修复门阻塞：必须修复PR27提交provenance硬编码，内置merge key dtype统一，冻结行为权威源与视觉属性连接契约，审定schema v6/v7及几何/ROI/padding等价性，并按38组完成重复泄漏检查。后续正式顺序固定为测量/QC与缺失机制 → NIR亮度/对比度、time-on-task、block/session order混淆审计 → pupil候选 → 重复场次稳定性与候选筛选 → 38组安全的组内/组间模型和参与者互斥预测；保留未调整与亮度+时间调整结果。未运行正式模型，未发布心理或生理结论。

## 当前加速口径追加（2026-08-29）

上述段落保留本次记录形成时的门控表述。依据当前任务指令，44场来源/字段/schema/provenance/QC全量预检已经完成，不再新增非必要前置门。正式适配器物化及统计当前只等待两项代码修复：`formal_evidence_commit`必须记录实际运行提交，且适配器必须内部统一`phase_segment`、`unix_ms`、`absolute_onset_time`、`next_trial_onset_time`等merge key dtype；两项均须在隔离工作树以1–2场回归验证。回归通过后直接扩大到44场既定测量、QC、NIR亮度/上一亮度/time-on-task审计、候选筛选、重复组模型和参与者互斥预测。此前的来源、身份、schema或几何审定文字不作为新增停止门；44场主口径仍为44个session、38个匿名分析组和6个双场重复组。

## PR28回归通过后的44场全量物化与结果验收（2026-08-29）

### 总结

用户确认PR28回归门已通过：`sub-031`和`sub-032`输出共163,383行，0重复键，`unix_ms`单调失败组为0，当前/上一刺激视觉属性在回归试次中1728/1728成功联结；10/1728个试次最近帧距离超过1000 ms，作为局部失败保留。随后立即完成44场全量适配器物化及下游NIR结果包，没有再次停留在preflight，也没有启动跨模态或正式报告效应解释。

### 原计划与执行

按照用户放行顺序，使用隔离工作树`D:\aaawork\07-竞赛\厚璨杯\验证工作树\NIR-PR28-98aee1e`（detached clean，HEAD=`98aee1e086c5c921b0ec73019830e63b873bb1a9`）、PR22行为正式表和权威44场NIR manifest，在`D:\Code\python\python.exe`下运行44场适配器及本地分析脚本。输入、代码、证据仓库、解释器、配置、输出根均记录在最终结果包manifest和日志；未pull、未push、未merge、未切分支，未修改生产代码仓库。

全量输出根为：

`D:\_AttentionData\Beijing-NIR\validation\formal44-pr28-run-20260829T150000\formal_output\nir-formal44-pr28-20260829T180000`

最终结果包物化38,016个行为试次对应的38,016行NIR trial特征、19,914行5秒time-on-task轨迹、44行session QC、30行（6组×5指标）重复组描述性稳定性和38组参与者互斥GroupKFold预测。当前刺激视觉属性38,016/38,016成功连接；上一刺激属性除每个session×block首trial的88行`not_applicable_first_trial`外，37,928/37,928成功连接。局部时间联结失败为19行，全部为`nearest_frame_gt_1000ms`，没有补零或静默删除。

### 验收与失败

最终文件级验收结果：trial主键38,016行唯一；44个session和38个匿名组均齐全；44行session QC的重复行和`unix_ms`单调失败均为0；输出表无PIR/iris字段；所有表均有CSV/Parquet；35个非哈希文件逐项哈希复核通过。4张图在本地打开检查后因运行环境中文字体缺字改为ASCII标签，数据未改变，哈希清单已重新生成。

5个混合模型（绝对、基线减除、相对基线、MAD变异性、趋势候选）均因`LinAlgError: Singular matrix`失败，已逐条写入`tables/failure_details.csv`，没有把空结果伪装成模型成功。参与者互斥预测5折均完成；其指标只作为工程审计产物，不解释为心理效应。双场重复稳定性每个组只有两个session、每个session两个block，相关仅作描述性检查，不能单独当作可靠性证明。

### 最终决策

1. 通过44场NIR来源适配器到trial特征的物化、主键/时间结构、QC分轨、当前/上一刺激亮度/对比度/大小/身份、time-on-task、候选指标、重复组表、参与者互斥折分、CSV/Parquet/schema/manifest/哈希和图表可追溯性验收。
2. 通过NIR主线的PIR/OAR拒绝：PIR不进入主线；未验证虹膜几何时不构造PIR/OAR；视觉属性只用于NIR前置审计，不扩散到毫米波或RGB。
3. 未通过5个混合模型的可估计性门；模型失败已保留为后续结构性修复任务，当前不发布任何正式心理或生理效应。
4. 当前结果包可交给正式统计/报告角色做设计复核，但不能把44个session写成44名独立参与者；正式报告必须继续使用44 session、38匿名组、6个双场重复组口径。

### 已完成、未完成与待确认

已完成：PR28回归后的44场适配器物化；NIR专属亮度/对比度/大小/身份连接；上一刺激内存派生；真实`unix_ms`时间匹配；缺失与局部失败分轨；瞳孔候选；重复组描述性表；参与者互斥预测；输出包和证据文档`NIR分析/1.6-NIR-PR28正式44场物化与审计结果包.md`。

未完成：5个混合模型的可估计性修复和定向复跑；正式统计角色对模型设计、质量阈值、缺失机制、窗口及未调整/亮度+时间调整结果的预先定义复核；正式报告效应解释。当前没有新增代码仓库修改，分析脚本和图表修复脚本均留在本地数据验证目录，等待专门Git任务按清单统一处理。

放行到正式44场报告分析的全部条件：混合模型设计矩阵与随机效应识别性修复并在隔离工作树回归通过；缺失/时间失败比例和质量轨道由正式统计角色复核；当前/上一亮度、对比度、大小、刺激身份、time-on-task、block/session order协变量及窗口冻结；6个重复组的稳定性不被两点相关过度解释；按38匿名组完成所有组内/组间模型和参与者互斥验证；保留未调整和亮度+时间调整结果；PIR/OAR不越过NIR专属边界；未来约72场接入时全量重建身份映射；报告角色依据完整manifest、schema、哈希、失败表和结果包另行批准发布。
