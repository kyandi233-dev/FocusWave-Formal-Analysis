# 跨电脑合并入口包（本机 08_算法 结果登记）

## 状态与用途

- 登记时间：2026-08-29（Asia/Shanghai）。
- 状态：`PARTIAL / CROSS-COMPUTER ENTRY ONLY`。
- 角色：专门 Git 任务；本记录只把另一台电脑上的既有结果包登记为可追溯的合并入口，不运行模型、不改分析定义。
- 本仓原主线不变：44 个正式候选场次、38 个当前队列匿名分析组和 6 个双场重复组仍以根目录 README 与本仓资产导航为准。
- 本入口的行为/probe canonical 分母为 70 sessions / 46 participants / 1,400 probes，与本仓 44 场队列不是同一已证实样本。没有已验收的 crosswalk 前，禁止把两套分母、会话或结果直接合并，也禁止把任何一方替换为另一方。

本文件不包含原始数据、视频、逐帧/逐探针/逐会话派生数据、模型权重或身份映射；仅保存本机路径、文件指纹、字段结构、聚合摘要和报告边界。路径为来源机登记，不表示接收机可以直接访问。

## 接收与合并前的必经门

1. 建立受控、去身份化的 44 场与 70 场 crosswalk；重复参与者须在同一分组折中，且身份映射不能提交本仓。
2. 对每个候选会话核对 `subject/session/block/probe/window` 与时间锚点；不得用行号或视频帧号替代时间键。
3. 重做键唯一性、block/probe 覆盖、时钟/单位、缺失原因与重复场次核验；字段不兼容或时间轴不可解释时停止合并。
4. 以统一样本冻结重新计算模型和报告表，不能平均两台电脑各自的 p 值、AUC 或其他性能。
5. NIR、RGB、毫米波分别通过下列模态门后，才可进入相应层级；原始输出和行级表保持在来源机/批准的大文件存储。

## 已登记来源包

| 模态/包 | 来源机路径 | 可复核清单或摘要 | 当前可用范围 | 禁止外推 |
|---|---|---|---|---|
| 行为/probe 与多模态入口 | `D:\\Project\\厚粲杯\\08_算法\\docs\\results\\final_merge_readiness_20260829` | `FINAL_MERGE_READINESS_20260829.md`；`available_model_sets.csv` SHA-256 `EE098350717437589C1144574C44379A4853385A6277729F5CD01D0C05286863`；`missingness_by_modality.csv` SHA-256 `749CADD93B54FBB3ADD23B25478C9370A62D64304506A8FB8C464C0DB8E9F334` | 70/46/1,400 的另一电脑 canonical anchor；仅作待 crosswalk 的入口 | 不替代本仓 44 场主线；不把传感器缺失改写为行为分母缩减 |
| 毫米波 formal QC v1 | `D:\\Project\\厚粲杯\\08_算法\\docs\\results\\mmwave_formal_vital_qc_v1` | 已脱敏 `MMWAVE_FORMAL_VITAL_QC_V1_REDACTED_MANIFEST.json` SHA-256 `9C4745F4836E9BDD886C829AB533151B66212C5659246B6EB05E108DB91EE076`；聚合 failure counts SHA-256 `50495020A4052D5E92CF62F9344B946B283E797226229632B6BF02DE8FEF5CC6` | 既有 QC/初筛与参考聚合证据；需 crosswalk 后才可映射到本仓候选会话 | 不称 HR/RR/HRV 已验证、生命体征可用或正式生理结论 |
| RGB raw/context 状态 v1 | `D:\\Project\\厚粲杯\\08_算法\\docs\\results\\rgb_current_status_v1` | `RGB_CURRENT_STATUS_V1.md`；字段字典 SHA-256 `524BA54122470708B4F335C170468E39DA5FD028CA5A62FB33A59FE9FAA78E5E` | raw/context merge ready 的字段与键设计；71/72 formal usable 是来源包内审计结论 | 不称 derived-window feature、downstream QC、眼动/眨眼/PERCLOS 或虹膜校正完成 |
| NIR 当前生产状态 | `D:\\Project\\厚粲杯\\08_算法\\01_Attention-Analysis_nvidia-cuda\\docs\\020-nir\\NIR_STATUS_TODAY.md`；输出根 `D:\\Project\\厚粲杯\\11_数据\\01_Attention-Analysis_nvidia-cuda_formal_NIR\\ritnet-fullclass-final` | `completion.json` schema v1；字段见下表；生产状态文档与输出根须同时复核 | 仅可作为 completion/alignment 待验入口 | 不把生产完成、历史 69/68 声明或来源预检等同于本仓 44 场正式 NIR 结论 |

## 聚合摘要与字段结构

### 行为/probe 入口

- A：behavior/probe baseline，70 sessions / 46 participants / 1,400 probes，canonical。
- B：behavior + RGB，70/46/1,400，RGB formal gate pending。
- C：behavior + mmWave preliminary screening，70/46/1,297；1,297/1,400 只是初筛 flag 子集。
- D：behavior + RGB + mmWave preliminary screening，70/46/1,297，辅助探索。
- E：加 NIR 的 completion-subject validation，68/44/1,259；须 exact subject/block/probe/onset alignment。
- 入口可合并表的键结构为 session-level 和 probe-level availability matrix；接收时需以受控 crosswalk 映射至本仓键，不能提交这些行级矩阵。

### 毫米波

- 已脱敏 manifest 声明的 formal QC 批次为 72 sessions：`Tier_1_QC_eligible_candidate=17`、`Tier_2_motion_only=53`、`Tier_3_unusable_for_formal_v1=2`。这是 QC eligibility/flag 分层，不是生理准确性分层。
- 独立参考聚合为 5 sessions、100 paired windows、99 metric rows。HR course MAE 4.5902 bpm、HR peak MAE 7.8216 bpm、BR peak MAE 11.7689 次/分、RMSSD MAE 262.6418 ms；只可作为有限的现有聚合参考，不能外推到 70 或 44 场。
- 字段/规则入口：`MMWAVE_FORMAL_VITAL_QC_V1_REDACTED_MANIFEST.json` 的 `session_counts`、`rules`、`reference_cohort`、`sources` 与 `outputs`；行级 session crosswalk、QC summary 和 failure-trigger audit 均不提交本仓。

### RGB

- 来源审计报告为 72 discovered、71 formal usable；`sub-099` 是非 formal usable，`sub-060` 缺 formal manifest。该计数不可直接映射为本仓 44 场。
- 合并键：`subject/session/block/probe/window`；时间锚点 `unix_ms`；同时保留 `video_frame_position` 与 `capture_frame_idx` 的 frame identity。
- 已登记字段类别：identity/context（behavior/probe 时间上下文）、face raw（bbox、68 点、head pose、AU、表情/affect、gaze、mesh、blendshape）、pose raw、motion raw，以及检测/时间间隔/掉帧/上下文完整性 QC 标记。
- 缺失的派生层：`rgb_features.parquet`、face tracking、eye/iris、pose features、blink/PERCLOS；不得从 raw 字段推断它们已完成。

### NIR

- 当前生产状态文档（2026-08-29）写明队列 `RUNNING`、当前 `sub-085`，并列出 22 个已完成 subject；它要求 `completion.json`、eye rows、frame rows、80 张 QC 图及 16 条 pixel evidence 才能登记 complete。
- 同一输出根本次只读枚举到 35 个 `completion.json`，均为 `status=complete`；其中 `sub-085` 的文件修改时间晚于状态文档所述未发布状态。此不一致未被本入口自行裁决：接收方必须以重新生成/验收的 completion manifest 和完整产物检查确定可用集合。
- `completion.json` schema v1 字段：`subject`、`status`、`eye_metric_row_count`、`frame_coverage_row_count`、`qc_saved_image_count`、`qc_pixel_evidence_saved_count`、`manifest_sha256`、`summary_sha256`、`work_identity_sha256`、`schema_version`、`total_output_bytes`、`output_limit_bytes`。
- 另一个既有入口包曾声明 fullclass 69 sessions 与 canonical 68-session intersection；该历史声明与本机当前生产状态分开保存，不能据此宣称本仓 44 场 NIR 完成。

## 报告边界

- 本入口是跨电脑对齐工作的登记，不是跨队列合并完成，也不是正式统计或报告结果。
- 本仓 README 冻结的 44/38/6 与该入口 70/46/1,400 必须并列呈现，直到 crosswalk、时间对齐、重复参与者处理和各模态门均通过。
- 毫米波只可进入 exploratory feature increment 或 quality-flag sensitivity；不把初筛 flag、17/53/2 或 1,297/1,400 解释成已验证生命体征。
- RGB 仅为 raw/context merge ready；不报告虹膜校正，且正式增量推断仍需 gate。
- NIR 仅可在经重新验收的 completion subjects 上进行 exact-alignment 子样本验证；不等待全队列，也不以 completion 当作生理或行为效度结论。

## 接收方最小验收记录

接收方应新增一条不含身份映射的记录，至少包含：来源包文件指纹复核、crosswalk 版本标识（不含内容）、键唯一性/覆盖结果、NIR completion 冲突处置、各模态纳入/拒绝理由、实际分析集分母、关联代码提交和重新生成的聚合结果。任何一项未通过时，维持 `PARTIAL` 并停止跨电脑合并。
