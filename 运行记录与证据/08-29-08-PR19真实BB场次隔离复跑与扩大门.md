# PR19 真实 BB 场次隔离复跑与扩大门

## 总结

本次作为本地行为数据执行任务，在 PR19 隔离工作树 `D:\AAAWORK\07-竞赛\厚璨杯\验证工作树\行为-PR19-0423beb` 上完成了不依赖目录发现的真实 BB 最小复跑。代码基线为 detached、干净的 `0423beba1813bf81b16d4d7d7e9c7ac4763920e6`，解释器为 `D:\Code\python\python.exe` / Python 3.13.0。

按阶段0.2纠正后的当前队列口径解释既有匿名映射，完整 manifest 只读预检仍为44个实验场次记录、38个当前队列匿名参与者分析组、6个双场重复组（12个重复session）。身份分组主依据为E2北京登记表姓名匹配，并以手机号/学号精确交叉核验；Q3 `phone_digits` 匿名键独立复核。旧R2 corrected registry只覆盖31/44个session，仅作历史/跨队列敏感性口径；13个R2外场次均是“当前队列单场分析组、未进入旧corrected registry”，不是身份未知。实际复跑显式列出 `sub-045`、`sub-163` 的B1/B2共4行，并透传同一匿名分析组；具体匿名组键仅保留在数据区运行输入和产物中，不写入 Git 文档。未纳入 `sub-9504`，未使用旧BBB，也未改写组号。

生产器成功生成试次、探针/固定秒窗口、phase/cycle、Block、session和错误轨迹表，共1,728试次、200窗口、96 phase/cycle、4 Block、2 session和301错误轨迹行。RT多尺度字段、Block/session层d′/c/β及log-linear校正、Q1/Q2原始编码、唯一键、QC和产物哈希均完成工程核验。

但真实数据发现：探针行的 `absolute_onset_time` 比 `probe_onset_time` 早约1.152–1.172秒，现有实现以 `probe_onset_time`作终点、以试次开始时间筛选，导致120个探针前窗口均包含对应探针锚定试次。该问题阻断窗口契约，**未通过扩大到44场正式推断门**。

## 原计划

1. 读取PR19隔离工作树入口、正式证据库行为分析1/1.1/1.2、分析设计和运行记录08-29-07。
2. 核验工作树状态、HEAD、解释器、冻结匿名映射和44场完整 manifest。
3. 在全新输出根用外部显式 session manifest 对1–2个真实BB场次复跑，不按目录猜队列、不纳入9504、不使用旧BBB。
4. 核验试次、探针/固定窗口、phase/cycle、Block、session、错误轨迹、RT多尺度、d′/c/β、Q1/Q2、唯一键、QC和哈希。
5. 保存命令、配置、manifest、schema、产物清单、行数和失败明细；未通过门控时停止，不运行44场正式推断。

## 执行与决策过程

### 1. 入口、基线和身份

已完整读取 PR19 工作树 `README.md`、`AGENTS.md`，正式证据库行为分析1、1.1、1.2、行为分析README、分析设计1和1.1以及运行记录08-29-07。PR19文档要求外部冻结 manifest、外部匿名组键、无硬编码队列规模和低机会数拒绝；正式证据库仍将实验场次记录与自然参与者分开。`subject_id`、`subid`、`experiment_id`均按session键解释；`presence`只表示模态输出存在性，不表示签到。

现场读取 `D:\_AttentionData\anonymous_participant_mapping_phase0_1_20260829\manifest.json` 和CSV。映射CSV为44行、38个当前队列匿名参与者分析组，6组各含2个session；E2为主分组依据，手机号/学号作精确交叉核验，Q3 `phone_digits`作独立复核；R2 corrected registry仅覆盖31/44，13个外场次按当前队列单场分析组处理。全局跨批次身份未冻结。所有44个场次均核验到B1/B2文件，选择 `sub-045` 与 `sub-163` 是为了在真实数据中检查重复场次共享匿名组键。

### 2. Manifest、配置和输出

根据冻结映射生成了只读预检用的88行完整显式 manifest，再生成4行真实最小复跑 manifest。完整 manifest SHA256为 `d5ce5cf1d9391617a76b8e02ea7399f20fd5da6758cbc663aaf18fd0e7d27144`；最小 manifest SHA256为 `f675e797be681488fb75c3dbba7eda67e5a50dedcefd5c82d692e2bc667d538d`；配置快照SHA256为 `b564d625fc96a89da27729da44da1a0adcc836a2d75284cbdbd2d2f71352b469`；冻结匿名映射CSV SHA256为 `a5579351194c741536a3f90041a5ff63b5c8f14c30a90abea5d8a4fb4bbcf47b`。

运行命令和所有输出路径集中记录在行为分析后续文档。全新输出根为 `D:\_AttentionData\output-v2\050-sart-formal-pr19-isolated\20260829_real_bb_sub045_sub163`，生产器拒绝覆盖既有目录并在清单中写入 `formal_statistics_run=false`。

### 3. 产物和指标检查

输出行数为：`trial_metrics.csv` 1,728；`window_metrics.csv` 200；`phase_cycle_metrics.csv` 96；`block_metrics.csv` 4；`session_metrics.csv` 2；`error_trajectory_metrics.csv` 301。所有主表唯一键通过；错误轨迹使用 `error_event_key + trial_offset` 复合键通过。试次层40个探针的Q1/Q2均非空且保留原始编码，本次实际Q1为1/2/3，Q2为2/3/4。

每个Block为384个Go机会和48个No-Go机会，每个session为768个Go机会和96个No-Go机会。Block/session的 d′、c、β均通过机会门并为有限值，log-linear校正按 `(count+0.5)/(opportunities+1.0)`复算一致。window和cycle层因低机会数全部显式 `rejected_low_opportunity`，没有将缺失替换为0。错误轨迹的46个 `not_calculable` 行均保留 `target_not_correct_go_rt` 原因。

### 4. 失败和纠偏

第一次普通 pytest 收集受到外部同名 `tests` 包遮蔽，出现 `ModuleNotFoundError: No module named 'tests.fixtures'`；临时 namespace 隔离后7个定向测试全部通过。测试不是本轮阻断原因。

逐表审计初版曾将原始路径前缀和Block机会数预期写错，修正审计逻辑后重新读取同一产物：原始路径检查通过，Block机会数改为384/48，唯一失败检查只剩探针锚定行排除。该审计脚本纠偏没有重跑生产器、修改输出或改变输入哈希。

真实窗口问题的证据是：120个探针前窗口均包含对应 `anchor_trial_key`；`probe_onset_time - absolute_onset_time` 为1.152–1.172秒，中位数1.155秒。失败明细、受影响窗口样例、测试命令和日志路径保存在输出目录 `audit/failure_details.json`。

## 最终决策结果

1. PR19真实最小复跑的试次、固定窗口、phase/cycle、Block、session和错误轨迹产物已生成并可追溯；本轮仅属于派生指标工程验证，不是正式科学统计。
2. 完整 manifest 预检通过44个session/38个当前队列匿名参与者分析组/6个双场重复组门控；实际复跑确实只使用2个显式session和1个既有共享匿名组。
3. RT多尺度字段和Block/session d′、c、β计算骨架可继续保留；低机会数状态、错误分轨、Q1/Q2原始编码和哈希记录符合契约。
4. 探针窗口锚定行问题为扩大门阻断项。必须先明确时间锚点并显式排除锚定试次，增加真实时间差测试，重新完成1–2场复跑。
5. 当前不得运行或发布44场正式推断，不得发布窗口RT效应、Q1/Q2关联、p值、AUC或报告结论。全局自然人身份与后续跨批次合并仍等待中央治理。

## 已完成、未完成和待确认事项

已完成：入口和治理文档读取；PR19工作树/解释器核验；阶段0.1匿名映射核验；44场完整 manifest 预检；2场真实BB隔离复跑；六类产物生成；schema、行数、唯一键、RT、SDT、Q1/Q2、QC、错误明细和SHA256留痕；定向测试的namespace隔离复核。

未完成：探针窗口锚点契约修订；真实时间差回归测试；修订后的1–2场复跑；44场标准化生产和任何正式推断。

待确认：由代码/监督任务决定采用 `probe_onset_time` 还是探针行 `absolute_onset_time`作为锚点，并批准“排除 `anchor_trial_key`”的最终规则；由样本冻结/中央任务确认后续是否允许扩大队列。

本次未修改代码仓库、原始数据、旧BBB输出或源报告，未删除/覆盖历史结果，未执行 pull、fetch、commit、push或切分支。上述两个新增文档和本地派生审计产物均保留未提交状态。
