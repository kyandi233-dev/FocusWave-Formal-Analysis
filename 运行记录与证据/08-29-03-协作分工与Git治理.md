# 协作分工与 Git 治理

## 一、总结

本轮完成正式分析仓库的协作角色、任务分工和 Git 治理入口建设。新增 `1.2` 作为职责与并发规则唯一权威，新增根目录 `AGENTS.md` 作为 Codex 稳定入口，并更新根 README、运行记录 README 和人工智能接手说明。仓库基线在修改前后保持清洁，本轮未运行正式分析、未修改关联代码仓库、未读取或复制原始数据。

## 二、原计划

### 背景

正式分析完整管线与报告证据库刚完成基础入口和资产/样本阶段记录。后续工作同时涉及本地数据读取与管线运行、网页 GPT 的科学分析和报告写作、监督分派，以及跨仓库版本控制。若没有明确边界，容易出现网页任务直接改 `main`、数据任务各自推送、跨仓库复制源码或把工程产物误写成科学结论的问题。

### 目标

1. 新增协作角色、任务分工、并发规则、跨仓库边界和 Git 治理的唯一说明。
2. 为本仓库增加稳定的 Codex 自动入口，只保留读取顺序和角色门控，不复制治理正文。
3. 更新根 README、运行记录入口和人工智能接手说明，使新任务能从入口到治理规则逐层定位。
4. 通过实际 Git 基线、链接、编号、编码和差异校验后，在无冲突条件下提交并推送到 `origin/main`。

### 步骤

1. 读取父项目规则、统一项目记忆、本仓库 README、运行记录 README、人工智能接手说明和近期工作记录。
2. 核验正式分析仓库的实际路径、分支、HEAD、远端、工作树和工作树列表；执行 `fetch --prune` 并确认远端基线。
3. 新增根目录 `AGENTS.md`、`运行记录与证据/1.2-协作角色、任务分工与Git治理.md` 和本工作记录。
4. 更新根 README、运行记录 README 和 `1.1-人工智能任务接手说明.md` 的入口链接、阅读顺序和必要门控段落。
5. 校验内部相对链接、说明文档编号、Markdown UTF-8 编码、敏感数据边界、Git 差异和提交前工作树。
6. 在本地 `main` 与 `origin/main` 一致且无未预期变化时，以中文范围清晰的提交信息提交并正常推送；推送后再次核验远端。

## 三、核验与发现

### 仓库基线

- 实际仓库：`D:\aaawork\07-竞赛\厚璨杯\040-竞赛报告与多模态分析\正式分析完整管线与报告证据库`。
- 初始分支：`main`；初始 HEAD 和 `origin/main` 均为 `d344b03b73ae6ee80c70a0f411decfe3b773cffd`。
- 远端：`https://github.com/kyandi233-dev/FocusWave-Formal-Analysis.git`。
- 工作树：仅有该正式分析仓库工作树；fetch 前后均无未提交变化。
- `git fetch origin --prune` 后，本地 `main` 与 `origin/main` ahead/behind 为 `0/0`，未发现远端新提交或需要合并的基线变化。

### 文件与规则发现

- 根目录原有 README、`.gitattributes` 和 `.gitignore`；没有现成根目录 `AGENTS.md`。
- `运行记录与证据`已有 `README.md`、`1-跨电脑复现与移植交付规范.md`、`1.1-人工智能任务接手说明.md`及两份 `08-29` 工作记录；新增 `1.2` 符合同一说明文档编号规则，新增工作记录使用 `MM-DD-NN-标题.md`。
- 父级规则要求先核验再修改、保留工作记录和历史、禁止未经同意删除/重命名；本轮没有操作父仓库的大量未提交变化，也没有删除或重命名既有文件。
- 本仓库 README 已明确代码权威位置、原始数据边界和正式批量分析尚未启动；本轮仅增加治理入口和说明，没有启动数据运行或修改源报告。

## 四、执行与决策过程

1. 将职责细节集中到 `1.2`，把本地数据执行、网页 GPT 科学/写作、监督任务和专门 Git 任务的输入、输出、禁止事项并列说明，避免把版本控制责任混入数据执行或科学写作。
2. 在 `1.2` 固定并发规则：网页 GPT 代码使用独立分支和 PR；本地数据任务不自行 `pull`、`push` 或提交；跨仓库源码留在权威仓库；本仓库只登记关联提交、运行记录和产物索引；所有任务开始前 fetch 并确认基线；冲突时停止，不 force push、不 reset 用户工作。
3. 新增根 `AGENTS.md`，只规定从根 README、模块 README、运行记录说明、最近工作记录到权威代码仓库的读取顺序，并指向 `1.2` 作为角色/Git 规则权威。
4. 更新三个既有入口文件，增加 `AGENTS.md`、`1.2` 和最新工作记录链接，补充任务角色声明、基线核验、停止提交/合并和交接要求。
5. 采用全中文正文；首次出现的 PR 写为“合并请求（PR，代码审查请求）”，fetch、Git、HEAD、main、origin、Codex 等版本控制专名按常用技术术语保留。

## 五、风险与控制

| 风险 | 控制措施 | 本轮状态 |
|---|---|---|
| 远端在修改期间新增提交 | 修改前 fetch，提交前后比较本地与远端基线 | 已通过；修改期间未发现新增提交 |
| 用户或其他任务已有未提交变化 | 修改前检查工作树；只操作明确文件；发现无法归属变化即停止 | 已通过；工作树干净 |
| 根入口与治理正文重复导致漂移 | 根 `AGENTS.md` 只保留稳定顺序和门控，职责正文集中在 `1.2` | 已执行 |
| 把工程成功误写成科学完成 | `1.2` 与 `1.1` 明确工程验证、探索性结果、不可评估和正式结论分轨 | 已执行 |
| 跨仓库源码复制或数据泄露 | 约束代码留在权威仓库，原始/行级数据留在数据位置，本仓库只存聚合证据和索引 | 已执行 |
| 说明文档编号或链接失效 | 按目录内 `1`、`1.1`、`1.2` 顺排，并执行相对链接检查 | 已通过 |

## 六、验证

已执行以下校验：

- 检查新增文件存在，且 `1.2`、工作记录和入口链接均指向实际文件。
- 扫描本仓库 Markdown 的相对链接，确认没有缺失目标。
- 检查 `1-`、`1.1-`、`1.2-` 顺序和工作记录 `08-29-01`、`08-29-02`、`08-29-03` 的命名。
- 以 UTF-8 读取新增和修改的 Markdown，确认无替换字符；正文为中文，英文仅保留必要的 Git/代码/路径术语，未写入原始数据行级内容。
- 查看 `git diff --check`、`git diff --stat` 和完整差异，确认只包含本任务指定文件。
- 提交前确认工作树干净、`main` 与 `origin/main` 一致；推送后核验远端 HEAD 与提交哈希一致。

## 七、最终结果

### 已完成

- 已新增根目录 `AGENTS.md`，作为 Codex 自动入口，正文以根 README 和 `1.2` 为权威。
- 已新增 `运行记录与证据/1.2-协作角色、任务分工与Git治理.md`，覆盖四类角色、输入输出、禁止事项、并发、跨仓库、Git 门控、科学边界、停止点和最小交接模板。
- 已更新根 README、运行记录 README 和 `1.1-人工智能任务接手说明.md` 的链接、阅读顺序和角色/Git 门控。
- 已新增本记录，保留本轮计划、核验发现、执行决策、风险、验证和未决事项。

### 提交与推送

本记录在提交前生成并随本轮文档提交；本轮最终提交哈希、推送状态和推送后远端核验结果以任务最终回传为准。提交条件为：本地 `main` 与 `origin/main` 无基线冲突、工作树只包含本轮文件、全部文档校验通过；若任一条件失效则停止提交并回传冲突，不自行合并。

### 未完成与待确认

- 本轮没有运行正式分析、没有读取或复制原始数据、没有修改任何关联代码仓库；后续数据任务仍需按 `1.2` 先完成角色和基线交接。
- 具体科学参数、质量门、窗口、模型和报告结论不在本治理记录中冻结，仍以对应分析设计、代码仓库和经核验运行记录为准。
