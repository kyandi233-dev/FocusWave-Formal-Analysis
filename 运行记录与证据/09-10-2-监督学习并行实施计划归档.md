# 监督学习并行实施计划归档

## 背景与目标

用户要求将增量修正后的《监督学习并行实施计划》写入 GitHub。总状态页基线为 `FocusWave-Formal-Analysis/codex/code-fix-ledger` 的 `eaa67523a6de03570d2c5914238102ec0d4e7440`，并指定 `1.15.5` 为唯一当前总状态页。

本任务只更新报告证据仓库的方法计划与工作记录，不修改 Attention-Analysis 代码、不运行统计、不修改源 Word。

## 归档决定

- 保持原有并行任务、集成顺序和验证框架。
- 训练流程、分析集合、行为接口和 NIR 接口并行推进；不等待全部特征代表量冻结。
- 覆盖率、总体有效率和缺失原因继续报告，不设置统一百分比自动删除。
- RT-CV 取消 `n>=20` 屏蔽；除数学可计算条件外，不新增经验性最低 n，除非后续有明确方法文献支持。
- 首轮 omission 唯一主输入为 `raw_go_omission_rate`。
- NIR 首轮主基础信号固定为 `pupil_geom_mean_diameter`；不重新开放面积、轴长、fraction 或 pupil/iris ratio 竞争主信号。
- 30 秒为主预测窗口；10/20 秒为敏感性；三个10秒只用于过程展示。

## 校验与交付

初次归档将计划追加到 `1.15.5`，用户随后明确指定并行计划应独立为 `1.15.7`。本次位置纠正恢复 `1.15.5` 的原样，并将计划移动到 `分析设计/1.15.7-监督学习并行实施计划_20260910.md`。提交前检查 Markdown、链接、差异范围和远端基线；提交后记录哈希与推送状态。
