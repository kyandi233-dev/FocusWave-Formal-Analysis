# 执行计划第二次修订

## 背景

用户确认本轮不是单纯写作，而是从资产核验、管线试跑、统计设计、44 个当前场次分析、可合并产物到新版报告的完整任务。本次修订还需要消除上一版中对远程光电容积描记、NIR 拓扑几何、行为结局、刺激亮度适用范围、图表和复现交付的不准确表述。

## 目标

1. 把工作目录改为不带编号的唯一入口。
2. 冻结单模态和融合模型的因变量层级，不强制合成唯一“专注分数”。
3. 把刺激视觉属性限定为 NIR 瞳孔模块的前置混淆审计，不机械加入其他模态。
4. 确认 031 主几何为拓扑规则版本，只保留紧凑的方法对比。
5. 移除远程光电容积描记分析路线。
6. 增加图表系统、跨电脑复现包和交给新人工智能任务的接手说明。
7. 使用正确的 Zotero 资料目录 `D:\ResearchManager`，不再使用默认用户库。

## 拟执行步骤

1. 核验官方报告模板、版权说明和评分表。
2. 核验 Zotero 活动数据库与 `2-厚粲杯` 集合树。
3. 修订总执行计划、资产地图和各模块入口。
4. 新增结局候选验证方案、图表台账、复现交付规范和人工智能接手说明。
5. 扫描旧路径、已放弃方法和未解释英文术语，完成一致性检查。

## 风险与校验

- Zotero 界面显示 91 条，活动数据库按子集合去重、排除附件后为 95 条。两个数字都保留，后续以具体文献条目键和标题建立证据矩阵，不依赖界面计数推断完整性。
- 模板 DOCX 已提取文本和样式，但本机无可用 LibreOffice 渲染器；最终生成报告时必须用可用的 Word 或 LibreOffice 逐页渲染验收。
- 任何源代码修改、批量运行、远程同步或删除操作均不属于本次“计划修订”，需要按后续阶段的审批门执行。

## 完成记录

### 总结

已完成工作目录更名、第二次执行计划修订、行为结局候选方案、图表台账、跨电脑复现规范、人工智能接手说明和官方报告模板/评分约束。本次只修改文档和入口映射，没有修改算法代码、原始数据或源报告，没有中断后台 NIR 任务。

### 执行与决策过程

1. 工作目录由带 `052-` 前缀改为不带编号的“正式分析完整管线与报告证据库”。
2. 根据用户最新命名规则，每个子目录的说明文档独立从 `1-` 开始，同组文档使用 `1.1-`、`1.2-`、`1.3-`；工作记录保留日期命名。
3. 行为指标改为跨尺度逻辑：RT 水平、离散程度、变异系数和斜率均可在探针窗口、区块和场次重算；d′、c、β 的使用取决于 Go/No-Go 机会数，而不是被固定绑定在某一尺度。
4. 单模态和融合模型的因变量明确分为试次、探针前窗口、区块/场次和 Q1/Q2 状态；传感器指标为预测变量块。
5. 刺激亮度、对比度、大小和上一刺激改为 NIR 瞳孔专属的前置审计，不默认加入毫米波和 RGB 科学模型。
6. 031 的主几何明确冻结为拓扑规则版本；其他几何只保留紧凑方法对比。
7. 远程光电容积描记退出当前 RGB 分析路线。
8. 已核验官方报告模板、版权说明和评分表，并将结构、字数、样式、统计表达、引用和复现要求写入报告约束。
9. Zotero 权威数据目录更正为 `D:\ResearchManager`，程序安装目录为 `D:\ZOTERO`。活动主库中的 `2-厚粲杯` 集合树已核验；本次没有通读文献正文。

### 校验结果

- 本工作目录共 19 个 Markdown 文件参与链接检查，内部相对链接缺失数为 0。
- 已扫描旧文件编号和 `052-` 工作目录引用；权威文档已更新为新路径和新编号。
- 已生成官方两个 PDF 的逐页图像并完成视觉核验。模板 DOCX 已读取文本和样式，最终报告生成阶段仍需使用 Word 或可用的 LibreOffice 进行逐页渲染验收。

### 未完成与后续顺序

1. 按资产冻结→行为候选验证→行为正式分析→NIR→毫米波→RGB→单模态/融合→报告的顺序实施。
2. Zotero 文献只在相应模块遇到具体问题时定向读取，不作全库预读。
3. 正式批量分析、代码修改、图表生成和报告改写尚未开始。
