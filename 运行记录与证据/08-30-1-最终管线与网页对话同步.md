# 08-30-1｜最终管线与网页对话同步

## 总结

本轮完成两条远端分支、两段网页对话和本机NIR生产目录的只读核验，并将最终代码主线、当前实现、剩余门槛、图表规则、Behavior遗漏分类、NIR三类参考值、方法章事实与下一步执行顺序写回正式分析证据库。未修改分析源码，未运行正式数据统计，未删除、覆盖或重命名历史文件。

## 原计划

1. 核验两个仓库的路径、分支、提交、远端和工作树。
2. 读取 `codex/code-fix-ledger` 新提交和 `codex/formal-analysis-v2-portable` 全部新增提交。
3. 读取两段网页对话的最新决定和未完成任务。
4. 对照代码、配置、测试和本地数据目录，区分已实现、待真实运行和待科学决策。
5. 更新资产导航、总执行计划、指标候选、图表台账、协作治理和方法章入口。
6. 检查差异、链接、文件编号、敏感数据边界和Git状态。

## 执行与决策过程

`FocusWave-Formal-Analysis@codex/code-fix-ledger` 本地工作树开始时干净但落后远端66个提交，已使用安全快进更新到 `1e6568dc76fed905f25ace94bccd8cbdacf971d5`。`Attention-Analysis` 主工作树位于七算法基准分支且存在未提交修改，因此没有切分支或清理，而是在 `验证工作树/Attention-Analysis-formal-v2-portable` 建立指向 `7b473782ffd8faf3b137a152117227341181d8d8` 的分离提交工作树审阅。

远端提交比较确认，正式便携分支以 `analysis/multimodal-integration` 的 `b2a96c0df6e6db88c599332815fb24cc93c07fe0` 为共同祖先，已完成便携路径、重复结构、Behavior科学第三版、NIR仅瞳孔适配、视觉/时间调整和失败门等多批提交。与此同时，当前配置仍使用 `repeat_participant_id` 和 FocusWave 3.1.3，网页最新资料则提出最终 `participant_key/visit_order/questionnaire` 接入及内部3.1.4口径；因此本轮只把它们记录为待核验/待实现，未静默修改科学配置。

网页对话还指出正式图不应内嵌题名、Behavior遗漏需拆型、三种瞳孔参考值不能混称基线。代码检索确认，当前正式图函数仍存在内嵌标题；Behavior主线仍主要使用程序原始遗漏，尽管部分NIR验证代码已经保存刺激前/延续按键相关的描述性遗漏亚型。因此计划中明确写成“已有局部资产，但统一正式合同尚未完成”，没有将其误报为完全缺失或已经闭环。

本机 `D:\_AttentionData\Beijing-NIR\amd-directml` 当前可识别44个唯一NIR场次级标识，故旧计划的27场预检状态已降为历史快照。该核验不等同于当前便携分支已经重建44场下游表、模型和图表。

## 最终决策结果

- 唯一正式下游代码主线：`Attention-Analysis@codex/formal-analysis-v2-portable`，核验提交 `7b473782ffd8faf3b137a152117227341181d8d8`。
- 唯一计划/证据/报告分支：`FocusWave-Formal-Analysis@codex/code-fix-ledger`。
- NIR上游44场资产已可见；下一步先冻结唯一来源并做1–2场完整试跑，再扩展44场。
- 毫米波不迁移到本机，由另一台正式电脑运行；本机只接入标准结果和5场结构性缺失。
- Behavior先完成最终身份/问卷层和遗漏互斥分类；NIR明确三种参考值；Behavior/NIR正式图建立无内嵌标题和覆盖矩阵合同。
- 正式报告下一步进入第4章方法；无伦理信息不补造，内部程序版本不要求写入正文，设备和协议事实从实际文件/源码核验。

## 已完成、未完成与待确认

已完成：远端安全更新、分支提交审阅、网页对话读取、本机NIR场次级标识核验、计划和入口文档修订。

未完成：身份/问卷和错误类型代码修改、无标题图表合同、图表覆盖矩阵、测试执行、真实1–2场试跑、44场全量重建、毫米波标准包接入和方法章正文。

待确认：正式程序3.1.3/3.1.4的实际差异是否影响数据/分析契约；最终全队列样本和 `participant_key` 映射；实验环境具体数值与练习试次实现；方法章如何如实表述知情同意而不虚构伦理审批。
