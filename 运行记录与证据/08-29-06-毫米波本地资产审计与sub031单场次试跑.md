# 毫米波本地资产审计与sub-031单场次试跑

## 总结

2026-08-29按“本地数据执行任务”边界完成毫米波代码、环境、输入和输出契约审计，并在隔离目录完成`sub-031`单场次最小试跑。当前队列身份门已冻结为44个场次、38个匿名分析组，其中6个为双场重复组；毫米波39个可加载场次与匿名映射联结后为33个当前队列分析组，仍含6个双场重复组；`global`跨队列身份尚未冻结。VMD主方法因当前解释器缺少`sktime`和`vmdpy`失败；`bp_heart`环境冒烟完成三段输出，但发现block1候选选择NaN和汇总字段错读。未运行44场次批量，未进行正式统计，未修改中央仓库或源报告。

## 原计划

1. 读取项目入口、正式分析计划、毫米波模块README、移植规范、资产导航和协作治理。
2. 核验`focuswave-central`当前分支、HEAD、远端差异、配置、脚本、机器包契约、解释器和依赖。
3. 盘点`E:\正式实验`和`D:\_AttentionData`中的毫米波输入、历史输出与可用参考设备。
4. 只在新输出根确认解释器和输入后运行一个场次，记录HR、RR/BR、IBI/HRV候选和质量字段。
5. 写入毫米波模块审计文档和本工作记录，明确合并字段草案、迁移建议、失败和正式放行条件。

## 执行与决策过程

### 1. 仓库治理与权限边界

已读取根`AGENTS.md`和`运行记录与证据/1.2-协作角色、任务分工与Git治理.md`。本任务固定为本地数据执行，不承担正式报告写作、跨仓库Git管理、科学定义修改或未经验证的HRV生理结论。核验`focuswave-central`：

- 路径：`D:\AAAWORK\07-竞赛\厚璨杯\021-analysisplan\focuswave-central`。
- 分支：`codex/mmwave-formal-reanalysis-v2`。
- `HEAD=d87229afe071f23450728a6d617ec82317e6c9df`。
- `origin/main=155a1aa9f38f9b9b7103c290660a1769e11dda7e`。
- `git rev-list --left-right --count HEAD...origin/main`为`42 5`，即当前分支领先42、落后5；只记录事实，不自行合并。
- 核验时中央工作树干净；本轮没有`pull`、`fetch`、切分支、`commit`或`push`。

主项目工作树本身已有其他并发任务的大量修改和未跟踪文件；本轮只新增本毫米波目录下的三份文档和本编号工作记录，不触碰并发任务占用的`08-29-04`、`08-29-05`记录。

### 2. 环境和代码检查

使用`D:\Code\python\python.exe`，版本为Python 3.13.0；numpy、pandas、scipy、statsmodels、sklearn、matplotlib、jsonschema可导入。`vmdpy`和`sktime`不可导入，因此VMD主方法不能放行。代码测试命令及结果：

```powershell
D:\Code\python\python.exe -m pytest -q tests/test_mmwave_benchmark_contract_v1.py tests/test_canonical_local_analysis_pipeline.py
# 17 passed in 3.14s

D:\Code\python\python.exe -m compileall -q scripts pipelines tests
# 通过
```

当前没有`configs/paths.local.json`；示例配置仍是旧路径。建议后续由代码/Git任务建立不入Git的机器本地路径配置，显式绑定`E:\正式实验`只读输入和`D:\_AttentionData`输出，但本轮没有创建配置。

### 3. 输入审计

`E:\正式实验`当前按场次级标识盘点44个目录：`031–055`、`061`、`137`、`141`、`144`、`146`、`150`、`153`、`156`、`163`、`164`、`165`、`167`、`168`、`171`、`172`、`173`、`174`、`176`、`177`。全部44个场次发现2个行为block文件。

39个场次具有可加载的基准NPZ、分片NPZ、有效时间戳和元数据；`036/038/040/041`的`.bin`仅32字节、timestamps为0字节，是无效占位，不属于可加载原始数据；`047`目录为空。当前代码的`INVALID_RECORDS`与此5个集合一致。当前队列为44场/38个匿名分析组，毫米波可加载队列为39场/33个匿名分析组；未来追加约72个场次必须重新生成全量匿名映射和合并键。

对`sub-031`进行结构核验：元数据`frame_count=277000`；基准NPZ加276个分片，每个分片数组形状为`(1000,256)`，合计277000帧；时间戳CSV同为277000行；复数数组有限值检查通过；基准和part001不相等。行为时间门保留baseline 19122帧、block1 57382帧、block2 56219帧。

每个正式场次目录内未发现ECG、RSP、Biopac或其他外部参考文件命名证据。因此HR/RR/IBI/HRV均不能标注为外部生理验证通过。

### 4. 实际试跑命令与结果

先用以下命令只生成时间门清单：

```powershell
$out='D:\_AttentionData\mmwave_local_execution\20260829_smoke_sub031_v311'
$env:PYTHONIOENCODING='utf-8'; $env:PYTHONUTF8='1'
& 'D:\Code\python\python.exe' -u scripts/run_timeline_gated_mmwave_quality.py --roots 'E:\正式实验' --subjects 031 --output-dir $out
```

结果为1个可纳入记录、2个任务block；之后使用同一路径尝试主方法：

```powershell
& 'D:\Code\python\python.exe' -u scripts/run_timeline_gated_mmwave_quality.py --roots 'E:\正式实验' --subjects 031 --output-dir 'D:\_AttentionData\mmwave_local_execution\20260829_smoke_sub031_v311' --run-analysis --method vmd_heart
```

该命令在baseline阶段失败，关键异常是`ModuleNotFoundError: No module named 'sktime'`和`ModuleNotFoundError: No module named 'vmdpy'`。目录保留3个部分产物、3908字节。

随后在全新输出目录以`bp_heart`确认输入—算法—输出链：

```powershell
$env:PYTHONIOENCODING='utf-8'; $env:PYTHONUTF8='1'
& 'D:\Code\python\python.exe' -u scripts/run_timeline_gated_mmwave_quality.py --roots 'E:\正式实验' --subjects 031 --output-dir 'D:\_AttentionData\mmwave_local_execution\20260829_smoke_sub031_bpheart' --run-analysis --method bp_heart
```

该命令成功完成baseline、block1、block2，共34个文件、23781276字节。三段HR融合值为82.2、83.3、81.2 bpm；BR时域值为19.4、19.0、18.9 bpm；均产生IBI/HRV候选。当前10秒位移信号硬门三段均为100%可用，但block1 HR时频一致性较低，且候选选择字段出现NaN，不能将这次运行标作整体工程通过。

### 5. 发现的输出契约问题

生产结果的字段为`breath_rate`、`heart_rate.time_course.signal_quality`和`heart_rate.time_course.metrics`；汇总器读取`breathing_rate`和顶层`quality`，导致`segment_analysis_summary.json`中相应字段为空。该问题需要中央代码任务修复并重跑，不能本地手工补列。

`bp_heart`运行的block1在`channel_selection.breath_gated[*]`中出现`best_hr_snr`、`best_hr_selection_score`、`best_br_snr`、`best_br_score`为`NaN`，伴随NumPy空切片/无效除法警告。原始复数输入抽查有限，故当前记录为算法选择和数值兜底问题，尚不归因为原始帧损坏。

### 6. 关键产物哈希

完整冒烟目录：`D:\_AttentionData\mmwave_local_execution\20260829_smoke_sub031_bpheart`。

| 产物 | SHA-256 |
|---|---|
| `crop_manifest.json` | `A695B65252FBAE410C9B97D0354CBAE745E80EFF17F50D81690CECA4D6E96CC4` |
| `crop_summary.json` | `43C0652E23C67F05C7060A84CC78A042B4661D66BEDA119A0A2B472CA9F83A49` |
| `segment_analysis_summary.json` | `E8918E3F9A7B87AB89ABD640883018AD6A0ABDA37F0BC8396180393FB3730272` |
| baseline vital JSON | `A5AB51B2B0BACB986FBB21CAEDE8E8EC405229B01F7497C1EF4B822DFA8ECB96` |
| block1 vital JSON | `B44D4673F347710DB162D4348471BB30E316D327FAB971CC08CE3FB63BF9D65F` |
| block2 vital JSON | `1E29AFA44BE39C04D1C2E536664338386FEB039710CF73C0F8AB8BA032FBBEF9` |

## 最终决策结果

本轮决策为“完成审计和单场次环境冒烟，正式批量阻断”。`bp_heart`结果只能证明当前输入和分段链可运行，不能替代VMD主方法、外部参考验证或正式报告证据。HR、RR/BR和IBI/HRV已在模块文档中分层记录：可计算不等于工程质量通过，工程硬门不等于外部生理验证，任何行为关联和报告使用均保持阻断。

## 已完成事项

- 完成规定入口文档、协作治理和毫米波模块审计。
- 完成中央仓库分支、HEAD、远端落后事实、配置和机器包契约核验。
- 完成E盘44场次级输入盘点、39/5输入状态分组、sub-031帧和时间轴核验。
- 完成主方法失败记录和`bp_heart`单场次隔离试跑。
- 写入`毫米波分析/1-当前管线与44场次输入审计.md`、`1.1-单场次试跑与质量门验证.md`、`1.2-后续样本合并与复现契约.md`。
- 保留两次试跑目录，未删除、未覆盖历史输出。

## 未完成与待确认事项

- 当前队列44场/38个匿名分析组及6个双场重复组已冻结，毫米波39场/33个匿名分析组已完成当前队列联结；`global`跨队列身份尚未冻结。未来约72场追加前必须重新生成全量匿名映射和全量合并键，不能直接沿用当前组数。
- 待代码任务修复`breath_rate`/`breathing_rate`汇总字段和质量字段权威位置。
- 待环境任务解决`vmdpy`/`sktime`依赖并确认正式方法。
- 待代码任务处理候选NaN/Inf和标准JSON校验。
- 待科学/数据负责人确认HR、RR/BR、IBI/HRV的工程门和外部验证方案；当前没有ECG/RSP，HRV不能作生理结论。
- 待上述事项通过后，以全新输出根复跑一个场次，再决定是否扩大；本任务不启动44场次批量。

## 交回上游的放行条件

只有当样本/重复身份冻结、44场次缺失输入有明确决定、固定解释器和方法可复现、NaN与字段契约修复、单场次重跑通过、机器包`merge_key`和列顺序确认、输出隔离和哈希记录齐全后，才可由负责人批准正式批量。任何需要最终身份映射、字段变化、中央代码修改、环境安装或多场次扩大运行的请求，均应回到相应治理任务审批。
