# 1.16.18 P5 Ocular 探针宇宙与 NIR 缺失身份桥接裁决（2026-09-13）

> 状态：`P5_IDENTITY_UNIVERSE_FROZEN / P5_REAL_PASS / NIR_STRUCTURAL_MISSING_ACCEPTED`  
> 上游：`1.16.17-P3P4真实关闭与P5三模态接口smoke计划_20260913.md`  
> 代码线：`Attention-Analysis/codex/1.16.10-modality-device-separation` / PR #68  
> P5 真实通过代码：`4205b8c903f33e372c1459a6186b4efa6a686293`

## 1. 触发事实与本地溯源闭环

P5 第一次真实接口运行暴露 Ocular 宽表缺少 canonical probe key（规范探针键）；第一次身份修复后再次真实物化时进一步发现，G1 `probe_measurement_candidates.csv` 只覆盖 2,180 个 probe，而 Ocular 宽表包含治理总体 2,320 个 probe。其差集为 140 个 probe，集中于 7 个场次，每场 20 个：`sub-086`、`sub-087`、`sub-088`、`sub-089`、`sub-090`、`sub-091`、`sub-099`。这些 probe 在 RGB 与 Behavior 中均存在，canonical identity 完整，但在 G1 中不存在，且其 NIR predictor（预测变量）全部为缺失。

本地只读溯源已经进一步闭环：

- `sub-086`～`sub-091` 共 6 场的原始 NIR 视频存在，但当前正式 producer 可用链中只有旧 schema 产物；旧产物缺少当前 fullclass-final/G1 依赖的 `hard_pupil_fraction`、`hard_iris_fraction`、`analysis_valid_pixel_fraction`、`pupil_geom_mean_diameter`、`soft_pupil_fraction`、`eye_metrics_schema_version` 等必需字段，因此不能作为当前 G1 合法来源；
- `sub-099` 原始 NIR 视频存在，但当前正式 producer 根目录中没有可用 producer 产物；
- 7 场均不是进入 G1 后被 QC 淘汰，也不是 probe identity 缺失；
- `nir_analysis_ready_source_manifest.json` 已显式记录 availability rule：NIR source missing 保持显式缺失，不改变 cohort membership（队列成员资格）；当前正式来源为 109 个可用场次 + 7 个 unavailable 场次 = 116 个治理场次。

因此这 140 个 probe 的性质正式确定为**结构性 NIR availability 缺失**，不是样本缺失、身份缺失或 QC 淘汰。

## 2. 正式裁决：Ocular 保持 2,320 probe 治理宇宙

Ocular science-output（科学输出）的 probe universe（探针总体）保持正式治理样本的 61 名参与者、116 个场次、2,320 个 probe。不同 Ocular 信息来源允许在同一规范探针宇宙中具有不同 availability：

- NIR pupil（近红外瞳孔）只在具有合法 G1 来源和通过相应 QC（质量控制）的 probe 上产生有限值；
- RGB blink（可见光眨眼）按其真实 RGB 可用范围产生有限值；
- 缺失 NIR 的治理 probe 继续保留规范身份，其 NIR predictor 保持 missing（缺失），不得补零、插补或伪造；
- 后续 analysis-set（分析集合）按具体比较所需特征形成 comparison-specific complete cases（比较特异完整案例），而不是在 science-output 层提前全局删除缺某设备的 probe。

因此不采用“把 Ocular 收窄到 G1 的 2,180 probe”方案。该方案会把设备/模态缺失错误转化为样本不存在，并改变已经冻结的 Ocular 覆盖分母。也不允许保留 2,320 行但规范身份为空，因为 P5 要求 canonical probe key 完整且无冲突。

### 2.1 7 场 NIR 不重跑的当前工程决定

考虑当前报告与正式分析时程，`sub-086`～`sub-091`、`sub-099` **不再启动新的 NIR producer 重跑**。现有 109 场正式 NIR 可用范围作为当前 Ocular/NIR 权威 availability；7 场按结构性缺失保留。

该决定只冻结当前工程执行边界，不把旧 schema 产物升级为当前合法输入，也不否认未来在独立扩展工作中重新生成这些场次的可能性。当前正式分析与报告必须使用现有真实 availability，不为追求完整覆盖而引入未经同一合同验证的新 producer 输出。

固定状态：

- `NIR_MISSING_7_SESSIONS = ACCEPTED_STRUCTURAL_MISSING`
- `NIR_PRODUCER_RERUN = NO`
- `COHORT_MEMBERSHIP_CHANGED = NO`
- `IMPUTATION = NO`
- `P5_BLOCKED = NO`

## 3. 身份来源与 fail-closed 规则

canonical probe key 固定为：

`participant_group_id + session_id + block_id + probe_index_in_block`

身份桥接采用多来源交叉校验，而不是把某一设备的科学测量值当作另一设备的数据来源：

1. G1 表为已有 NIR 候选的 probe 提供并验证规范身份；
2. 已纳入正式实验治理范围的完整 probe-level 表可为 G1 缺失 probe 提供**身份信息**；当前实现复用已进入 Ocular builder 的 RGB strict probe 表；
3. 两个来源重叠的 probe 必须在 session / participant group / block / probe index / `probe_event_id` 上一致，否则 fail-closed；
4. 补充身份来源只补 identity（身份），不得填充任何 NIR pupil predictor；
5. `probe_event_id` 保留为 provenance（来源追踪）字段，并与规范键逐行核对。

该规则实现的是“统一身份宇宙 + 模态特征按真实可用性缺失”，不改变任何已冻结的科学表示或测量阈值。

## 4. P3/P5 真实关闭结果

P3 的科学冻结结论不变：hard `R_seg`、`level_mean`、`variability_mad`、20 s 最低时间跨度、early/late support、linear slope 与 quadratic curvature 均保持原决定。

在 `Attention-Analysis@4205b8c903f33e372c1459a6186b4efa6a686293` 上，本地已经成功重新物化 Ocular 并重新运行 P5：

- Ocular wide：2,320 行；canonical key 缺失 0、重复 0；
- G1 覆盖 2,180 probe，G1 缺失治理 probe 140；
- 140 行 canonical key 全部完整，24 个 NIR predictor 的有限值数均为 0；
- hard `R_seg + rgb_nir_qc`：slope finite = 1,857；curvature finite = 1,855；
- 20 s、early/late、hard `R_seg`、`level_mean`、`variability_mad` 均未变化；
- P5 `status = pass`；
- Behavior / Ocular / Movement 三模态 canonical key 两两及三模态共同范围均为 2,320 probe / 116 sessions / 61 participant groups；
- predictor audit 中不存在缺失的 registry-ready predictor、time-legality 错误或 required-devices 错误；
- 缺失策略保持 `audit_only_no_zero_fill_no_imputation`；
- Behavior RT mean/median 的研究者冻结仍为 warning，不自动选择。

P5 停止线保持：

- `final_feature_registry_mutated = false`
- `supervised_model_run = false`
- `multimodal_model_run = false`
- `performance_metric_computed = false`
- `stop_after_smoke = true`

因此本轮三模态 P5 已正式关闭；该通过只证明接口、身份、时间合法性和缺失语义一致，不构成监督学习性能结果。

## 5. 本地目录与 Drive 权威证据

第二次失败曾因 `replace=True` 使本机 `FormalScience/Ocular/` 暂时处于未完成冻结后处理的降级现场；该事件保留在历史 BLOCKED bundle 中。`4205b8c...` 成功重物化后，本机 `FormalScience/Ocular/` 已恢复为当前权威冻结状态。

当前 P5 通过 bundle：

`_AI_HANDOFF/2026-09-13_p5-interface-smoke-pass-4205b8c/`

该 bundle 与两次失败 bundle 并存、不覆盖：

- `2026-09-13_p5-interface-smoke-BLOCKED-dd94a13/`
- `2026-09-13_p5-interface-smoke-BLOCKED-2-13dbead/`
- `2026-09-13_p5-interface-smoke-pass-4205b8c/`

后续结果账与报告应引用 pass bundle 作为当前 P5 权威证据；失败 bundle 仅用于 provenance（追溯）。

## 6. 后续边界

1. 不重跑这 7 场 NIR producer；109 场 NIR availability 作为当前正式事实。
2. 不因为 7 场 NIR 缺失改变 116 sessions / 61 participant groups / 2,320 probes 的治理总体。
3. Ocular 下一步是冻结后正式解释性分析与图件（任务进程、Q1、Q2、近期 Behavior），不是重新开放 P3 特征选择。
4. P5 已完成，不因 Ocular 图件或 Cardiopulmonary（心肺）尚未完成而回滚。
5. unified feature registry（统一特征登记表）、mini-LOSO（小规模留一参与者验证）和正式监督学习仍需等待剩余研究者冻结与相应方法闸门。
