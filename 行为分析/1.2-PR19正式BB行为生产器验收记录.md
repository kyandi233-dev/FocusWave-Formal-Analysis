# PR19 正式 BB 行为生产器验收记录

## 审查范围

审查仓库 `focuswave-multimodal-attention-analysis` 的分支 `codex/formal-bb-behavior-v1`，头提交为
`0423beba1813bf81b16d4d7d7e9c7ac4763920e6`。Git 合并引用核验的目标基线为
`155a1aa9f38f9b9b7103c290660a1769e11dda7e`。审查在独立临时 worktree 完成，未切换或污染用户现有工作树，未合并、未推送。

## 实际核验

- 生产器只接受外部冻结 session manifest 和匿名身份映射，不按目录或编号区间发现队列，不硬编码 44 场或 38 组。
- 每个选中 session 必须有非空 `anonymous_participant_group_id`；旧 BBB/`050-sart-formal` 路径和不允许的 source contract 被拒绝。
- 产出 trial、探针/固定秒 window、phase/cycle、block、session 和错误轨迹多尺度指标。
- RT 指标按尺度计算；d′、c、β 使用 log-linear 修正，并在 Go/No-Go 机会数不足时拒绝估计。
- `GroupKFold` 以匿名参与者组为分组单位，重复 session 不跨折；插补、标准化和特征选择只在训练折拟合。

使用 `D:\Code\python\python.exe`（Python 3.13.0）运行定向测试，结果为 **7 passed**。本机同名 `tests` 包造成普通收集导入遮蔽，未修改仓库，使用临时 namespace 隔离后完成测试。fixture 仅 4 个 session、3 个匿名组，不等价于 44 场真实运行。

## 验收结论与复跑门

**代码验收通过，可允许本地最小复跑。** 复跑落点为
`codex/formal-bb-behavior-v1@0423beba1813bf81b16d4d7d7e9c7ac4763920e6`，必须使用冻结的 44 场外部 manifest 和匿名组映射。

**正式科学准入未通过。** 复跑必须核对 44 个候选场次、38 个当前匿名分析组和 6 个双场重复组；旧 BBB 输出不得冒充当前 BB 结果。

